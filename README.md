# Invest — 台股成交值排行

個人使用的投資研究網頁系統。第一個功能是「個股成交值排行」，用來判斷哪些標的近期正在吸收市場成交值。
族群分類會在串接 Google Sheets 之後加入。

## 目前階段

**階段一：計算引擎與互動網頁**

- 行情資料是**證交所與櫃買中心的真實每日收盤行情**，由 `Infrastructure/MarketData` 逐日下載後快取在本機。
- 排行榜上的每個數字都由原始成交值即時算出，沒有任何預先寫死的結果，公式全部有單元測試釘住。

尚未實作：SQLite / EF Core、Google Sheets API 與族群分類、圖表、60 日期間。

## 環境需求

- macOS
- .NET 10 SDK
- VS Code + C# Dev Kit

## 如何執行

```bash
cd /Users/frankchiang/Desktop/Application/Code/Project/Invest

dotnet build
dotnet test                                # 驗證計算公式

# 第一次執行必做：回補行情。逐日下載並快取到 data/imports/
dotnet run --project src/Invest.Web -- backfill 70

dotnet run --project src/Invest.Web        # 啟動網站
```

啟動後 Terminal 會印出網址（例如 `https://localhost:7xxx`），排行頁在 `/trading-value-ranking`。
按 `Ctrl + C` 停止。

### 關於回補

```bash
dotnet run --project src/Invest.Web -- backfill [交易日數] [起始日期]
```

- 官方 API 一次只給一天，所以回補是「從起始日往回走，每天打一輪請求」。
  一輪包含兩個市場的收盤行情，加上七份用來扣除非一般交易的報表（詳見下一節）。
- 已下載過的日期會直接略過，因此這個指令可以重複執行，中斷後再跑會從斷點繼續。
- 非交易日也會被記錄下來，不會反覆重打同一個沒有資料的日期。
- 每次請求之間有 3 秒延遲，避免被官方網站擋。70 個交易日大約需要 15 分鐘。
- 交易日數的下限取決於要看的期間：成交熱度需要 2N 天，資金加速需要 3N 天。
  20 日 × 資金加速 = 60 個交易日，這也是預設回補 70 天的理由。

快取檔放在 `data/imports/yyyy-MM-dd.json`，不進版控。
檔案裡的 `schemaVersion` 記錄成交值的定義版本，定義改變時舊檔會被回補指令視為過期並自動重下，
避免新舊定義混在同一份排行裡。

### 成交值只計一般交易

官方「每日收盤行情」的成交金額註明**含一般、零股、盤後定價、鉅額交易**，
但市場上看到的成交值排行（玩股網、籌碼K）只算一般交易。兩者差距在高價股上特別大——
2026-08-07 的台積電就差了 59 億，足以改變名次。

因此回補時會另外抓下列報表，逐檔扣除：

| 市場 | 扣除項目 | 報表 |
|---|---|---|
| 上市 | 盤中零股 | `afterTrading/TWTC7U` |
| 上市 | 盤後零股 | `afterTrading/TWT53U` |
| 上市 | 盤後定價 | `afterTrading/BFT41U` |
| 上市 | 鉅額交易 | `block/BFIAUU` |
| 上櫃 | 盤中零股 | `afterTrading/oddQuote` |
| 上櫃 | 盤後零股 | `afterTrading/odd` |
| 上櫃 | 鉅額交易 | `blockTrade/quote` |

上櫃的每日收盤行情標題就寫著「不含定價」，所以上櫃不需要扣盤後定價。

排行只收**一般股票**：代號為四位數字且不以 0 開頭。ETF、權證、受益證券、特別股都排除在外。

## 發佈成靜態網站

排行頁是 Blazor Interactive Server，每次點按鈕都要回伺服器算，本機不開就看不到。
為了在手機上隨時查看，`export` 指令會把所有篩選組合先算好，輸出成一份純靜態網站：

```bash
dotnet run --project src/Invest.Web -- export publish/site
```

產出 960 個 JSON（10 基準日 × 4 期間 × 2 模式 × 3 市場 × 4 門檻）加上 `index.html` / `site.css` / `site.js`，
整包約 43 MB，但單頁只載入其中一個約 50 KB 的檔案。20 MB 的原始行情不會上網。

基準日只輸出最近 10 個交易日：每多一天就是一整份 96 種組合（約 4.5 MB），全部 75 天會膨脹到三百多 MB。
上限定義在 `RankingDates.SelectableTradingDayCount`，排行頁與靜態網站共用同一份日期清單。

**計算只有一份**：exporter 直接呼叫 `TradingValueRankingQueryService`，
連顯示文字都用 `RankingFormatter` 格式化好才寫進 JSON，
前端只負責挑檔案、畫表格與排序，不重寫任何公式。

發佈到 GitHub Pages 用 `gh-pages` 分支，每次覆蓋整個分支，
避免每次更新都把幾 MB 的產出永久留在 git 歷史裡：

```bash
tmp=$(mktemp -d) && cp -R publish/site/. "$tmp/" && touch "$tmp/.nojekyll" && cd "$tmp" \
  && git init -q -b gh-pages && git add -A && git commit -qm "更新排行快照" \
  && git remote add origin git@github.com:qwe953751/Investment.git \
  && git push -qf origin gh-pages
```

網址：<https://qwe953751.github.io/Investment/>

這是一份快照，不會自己更新——**回補完行情就要接著 `export` → 發佈**，
否則手機上看到的還是上一次的數字。

前端抓 `data/*.json` 時會帶上 `manifest.json` 裡的版本號（發佈時間的 Unix 秒數）當查詢字串，
`manifest.json` 本身則以 `cache: 'no-store'` 讀取。重新發佈後版本號一變，所有資料檔的網址跟著變，
瀏覽器手上的舊快取自動失效，不必手動清快取。

## 專案結構

```
Invest/
├── Doc/                                原始需求與交接文件
├── data/imports/                        行情快取（不進版控）
├── publish/site/                        靜態網站產出（不進版控）
├── src/Invest.Web/
│   ├── Components/                      版面、共用頁面
│   ├── Domain/Stocks/                   跨功能共用的核心模型
│   ├── Infrastructure/MarketData/       證交所與櫃買中心的行情下載與快取
│   │   ├── Twse/
│   │   └── Tpex/
│   ├── Infrastructure/StaticSite/       靜態網站輸出（Assets/ 是頁面本體）
│   └── Features/TradingValueRanking/    成交值排行功能
│       ├── Models/                      查詢條件、結果模型、顯示格式
│       ├── Pages/                       排行頁
│       └── Services/                    計算器與查詢服務
└── tests/Invest.Web.Tests/              計算公式的單元測試
```

架構原則：單一 Solution、單一 Web Project，以 `Features/` 目錄切分業務功能，不為每個功能建立獨立 `.csproj`。
`Domain/` 只放跨功能共用的核心模型，`Infrastructure/` 只放外部整合，計算邏輯不寫在 Razor 頁面裡。

## 核心指標定義

| 指標 | 公式 |
|---|---|
| 平均每日成交值 | 期間總成交值 ÷ 期間交易日數 |
| 成交值增減率 | （本期平均 − 前期平均）÷ 前期平均 |
| 市場成交比 | 個股期間成交值 ÷ 全市場期間成交值 |
| 排名變化 | 前期排名 − 本期排名（正數代表上升） |
| 期間漲跌 | （期間終點收盤價 − 基準收盤價）÷ 基準收盤價 |

- 「前期」固定採緊鄰的同長度區間（例如近 20 日 vs 再往前 20 日）。
- 市場成交比的分母一律是上市＋上櫃全體，不隨畫面上的市場篩選改變，這樣切換市場時比例才能互相比較。
- 前期平均為 0 時增減率無法計算，顯示 `—` 並排在最後，不會被當成 0%。
- 期間漲跌的**基準收盤價是進入期間之前的最後一個收盤價**，不是期間內第一天的收盤價。
  用期間內第一天當基準的話，那一天自己的漲跌會被吃掉；而且看「前一交易日」時起點等於終點，
  漲跌會永遠是 0%。期間之前完全沒有收盤價（例如期間內才上市）時顯示 `—`。

觀察期間可選 **前一交易日 / 5 / 10 / 20 日**，預設 5 日。
**基準日**是觀察期間的最後一天，預設最新的交易日；往回選一天，該日之後的行情就完全不列入計算，
因此看到的會是當天收盤後的排行。日期按鈕按日曆連續排列，週末、假日與尚未回補的日子以停用的按鈕留在原位。
60 日需要至少 120 個交易日的歷史資料，目前的回補量還不夠，按鈕暫時停用。
盤中即時行情列為之後的優化項目，目前只有收盤後的完整交易日資料。

## 排行模式

| 模式 | 排序依據 | 回答的問題 | 需要的資料量 |
|---|---|---|---|
| 成交熱度 | 本期平均每日成交值 | 最近哪些標的吸收最多成交值 | 2N 個交易日 |
| 資金加速 | 成交值增減率 | 哪些標的的成交值相較前期快速放大 | 3N 個交易日 |

資金加速需要 3N 是因為它的「前期排名」本身也是一個增減率，前期還需要自己的基期。

畫面上的**成交門檻**是「平均每日成交值」的下限，可選 不限 / 5000 萬 / 1 億 / 10 億，預設 5000 萬。
按鈕上顯示的是乘上期間天數後的總額（例如 5 日 × 5000 萬 → 2.5 億），這樣才能直接跟表格上的金額對照；
條件本身沒有變，平均每日 ≥ X 等同期間總額 ≥ X × 天數。

門檻主要是為了資金加速模式：冷門股從幾十萬跳到幾百萬就是好幾倍成長，
不過濾的話排行榜會被這類沒有實際意義的標的佔滿。

## 設計原則

優先順序：計算正確 > 資料可追溯 > 歷史結果穩定 > UI 可查詢 > 視覺效果。

歷史結果必須穩定：日後接上資料庫與族群分類時，每日計算結果會固化保存，
不可因為今天修改了族群成分股，就讓過去幾個月的歷史排名被重新計算而改變。
