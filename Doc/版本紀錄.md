# 版本紀錄與跨裝置交接

這份文件是其他電腦、手機或 AI agent 接手時的第一個版本入口。
它記錄已發布的功能、Git 提交、資料庫 migration、驗證結果與下一步；完整公式仍以
[README.md](../README.md)、進度以[完成進度.md](完成進度.md)、未完成討論以 [TODO.md](../TODO.md) 為準。

## 2026-08-20：指數與日 K 功能發布

| 項目 | 已發布內容 |
|---|---|
| `main` commit | `9bb1c785a8a9f66eddcc5fe0a52b563d6c42b60d` |
| GitHub Pages 快照 | `gh-pages`，版本 `1787211165` |
| 正式網址 | <https://qwe953751.github.io/Investment/> |
| .NET | `net10.0`，SDK `10.0.302` |
| 測試 | 82 個，全部通過 |
| 更新間隔 | 盤中每 2 分鐘；指數與展開中的 K 棒共用同一個設定 |
| 資料庫 | `db/007_market_indices.sql`、`db/008_intraday_kline.sql` 已套用 |

### 已完成的功能

- 盤中與盤後摘要都顯示加權指數、櫃買指數、指數值與漲跌幅。
- 點擊排行中的標的名稱，在原列下方展開小型日 K，不覆蓋原本排行內容。
- 日 K 顯示檢視日期往前三個月的官方 OHLC。
- 日 K 使用向前還原權息；同一事件倍率同時套用開、高、低、收。
- K 線繪製 MA5、MA20、MA60、MA240；均線使用有效交易日收盤價。
- 盤中追加 MIS 當日尚未收盤的 K 棒；收盤價使用盤中最新價。
- 盤中指數與展開中的標的 K 棒使用同一個 2 分鐘更新週期。

### 已驗證的結果

- 本機 `localhost:5199` 頁面、`site.js`、`data/kline/2330.json` 都回傳 HTTP 200。
- 公開 Supabase API 可讀取盤中 `intraday_latest` 的指數與 OHLC 欄位。
- 2026-08-20 MIS 最新輪次共 1,972 檔；完整 OHLC 1,950 檔，無成交的 22 檔保留空值，不捏造價格。
- 測試命令：

  ```powershell
  & "$env:LOCALAPPDATA\Microsoft\dotnet\dotnet.exe" test tests\Invest.Web.Tests\Invest.Web.Tests.csproj -c Release --no-restore -m:1 --disable-build-servers
  ```

## 其他裝置接手流程

1. 先切到最新主線並確認 commit：

   ```bash
   git switch main
   git pull --ff-only origin main
   git log -1 --oneline
   ```

   預期最新 commit 開頭是 `9bb1c78`。

2. 先讀這幾份文件：

   - [README.md](../README.md)：功能、公式與日常指令。
   - [完成進度.md](完成進度.md)：目前已完成項目與踩過的坑。
   - [開發環境.md](開發環境.md)：.NET 10、PATH、權限與本機預覽。
   - [TODO.md](../TODO.md)：尚未開始或仍在討論的工作。

3. Windows 先確認 .NET 10：

   ```powershell
   & "$env:LOCALAPPDATA\Microsoft\dotnet\dotnet.exe" --version
   ```

   預期為 `10.0.302`。若 `dotnet` 命令仍指向 .NET 8，使用上面的絕對路徑；不要以 .NET 8 建置本專案。

4. 修改程式碼前，確認 `db/008_intraday_kline.sql` 已存在；若是新資料庫或新環境，依 README 的 migration 順序套用，不能用假資料跳過。

5. 修改畫面或計算後，先跑測試，再重新 export；正式網站使用 `gh-pages` 快照，不會自動讀取 `main` 的原始碼。

## 下一個尚未開始的功能

「自訂」篩選頁尚未開發，需求已記在 [TODO.md](../TODO.md) 第 10 項：單一交易日、可瀏覽全部股票、超過 100 筆要分頁、收盤價／營收／成交值、日期選擇與成交值下限，且不做預設排行。
