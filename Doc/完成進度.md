# 完成進度

## 說明

換一台裝置接手時先讀這份：**到目前為止實際做完了什麼、現在是什麼狀態**。

- 功能怎麼用、公式怎麼算 → [README.md](../README.md)
- 為什麼長成這樣 → [專案計劃書.md](專案計劃書.md)
- 還在談、還沒做 → [TODO.md](../TODO.md)
- 要在新裝置上開工 → [開發環境.md](開發環境.md)
- 每一次改動的詳細記錄與跨裝置交接 → [版本紀錄.md](版本紀錄.md)

**維護規則**：這份文件只保留固定的 6 個小節，不再新增其他標題——
說明、現況摘要、排程 Action、最新已發布版本、已經能做的事、踩過並且已經解掉的坑。

- 任何帶日期的異動記錄或除錯過程，一律寫進 [版本紀錄.md](版本紀錄.md)，
  標題格式固定為 `[時間][地點||電腦系統||AI平台][標題]`，不寫在這份文件裡，
  細節見 [版本紀錄.md](版本紀錄.md) 的「更新規則」。
- 內容不屬於上述 6 類時：能併入既有小節就併進去；屬於歷史異動或除錯過程就搬去
  版本紀錄.md；屬於環境建置步驟就搬去 開發環境.md；都不屬於、又已經處理完畢的就直接刪除。
  不為了容納這類內容而新增第 7 個標題。

最後更新：2026-09-05。這份文件不會自動更新，內容跟 repo 對不上時以 repo 為準。

## 現況摘要

| 項目 | 數字 |
|---|---|
| 開發起始 | 2026-08-08 |
| 行情快取（`data` 分支 `imports/`） | 313 個交易日（2025-05-21 ~ 2026-08-28） |
| Supabase 交易日 | 300 個（2025-06-10 ~ 2026-08-28） |
| Supabase 列數 | 580,956（盤後行情） |
| **Supabase 空間用量** | [**156 MB / 500 MB＝31.2%**](https://supabase.com/dashboard/org/tjxmwnpltlntapjrlgtf/usage)（2026-08-28 21:30 查證，盤中資料尚未被隔日 sync 清掉） |
| 股票檔數 | 1,990 |
| 月營收 | 原始表 66 個月（2021-02 ~ 2026-07）；彈窗用的 `revenue_history` 共 39,448 列 |
| 盤中量能曲線 | 16 個交易日；已達門檻，正式用於盤中成交值校準（筆記 #42，2026-09-05） |
| 重大訊息（`material_events`） | 1,876 則；每日排程累積 |
| 單元測試 | 390 個全綠（本機 .NET 10.0.302 實跑，2026-09-05） |
| 最後一份原生資料備份 | `data` 分支 `snapshots/2026-08-28.sql.gz`（08-24～08-27 因 `db/012` 漏授權而缺，詳見 [版本紀錄.md](版本紀錄.md) 的「`db/022` 修好從 08-23 起就壞掉的備份」一節） |
| 正式網址 | <https://frank-invest.github.io/>（單一網址，訪客模式為預設，監控者／最高權限需登入） |
| 監控者網站 | `https://frank-invest.github.io/?key=<監控者密碼>`（`<監控者密碼>` 換成監控者帳號的實際密碼）——網址帶密碼開頁就自動登入監控者模式，不用手動打字，適合做成連結／捷徑給長輩用；用過一次密碼就會自動從網址列清掉，不會留在截圖或瀏覽紀錄裡（機制見 site.js 的 `AUTOLOGIN_QUERY`）。監控者視角的族群列表只看得到一個大族群（預設「伺服器」），不顯示搜尋框與篩選按鈕 |

`qwe953751.github.io/Investment/` 維持空白頁，不對外顯示排行資料，只是網址保留不刪。
`admin888/`、`viewer/` 這兩個過渡期網址已收掉（詳見 [版本紀錄.md](版本紀錄.md) 的「收攏成單一網址，admin888／viewer 收掉」一節）。
正式網站根目錄已由 `daily-snapshot.yml` 的 publish-only 流程更新至快照 `1788537783`；Podcast／股癌 UI
已納入正式筆記頁的子頁籤，最高權限進入 `?view=notes&notesTab=gooaye` 即可查看研究分析與 Podcast 來源樣版。
本機工作樹已移除股癌頁面的硬編碼示意資料；來源頁只接受時間、集數與 Gemini Notebook 逐字稿分析，
來源清單可在目前瀏覽器本機新增、編輯、移除，研究分析頁則依實際匯入資料產生時效權重、族群／標的與市場內容。
本批修改尚未重新發布 GitHub Pages；正式 Gemini／API／Supabase 資料流程仍未接通。

## 排程 Action

目前有 5 條會自動重複執行的 GitHub Actions，涵蓋台股盤中／盤後、美股、月營收、備份還原測試。
`intraday.yml` 跟其他四條不一樣：它不是單純的 cron，而是**自走鏈**——每一輪跑完自己呼叫
`workflow_dispatch` 排下一棒，YAML 裡的 `cron` 只是「萬一鏈斷了」的復原用火種，不是主要觸發方式
（原因見 `intraday.yml` 檔頭「為什麼是自走鏈而不是排程」一節：GitHub 原生 cron 實測誤點過
6～13 小時甚至完全沒觸發）。

| Workflow | 觸發方式 | 實際執行時間（台北） | 做什麼 |
|---|---|---|---|
| `intraday.yml`（台股盤中） | 自走鏈：每輪完成後自己叫下一輪；cron 只是斷鏈時的復原備援 | 交易日 08:40～13:35，每約 2 分鐘一輪 | 抓盤中報價寫 Supabase；18:00 後若當天 `daily-snapshot.yml` 還沒成功會自動叫醒它；另一個 job 監看盤中 CDN（Supabase Storage）健康度，掉回資料庫直連會發警報 |
| `daily-snapshot.yml`（台股盤後） | cron 16:07 進場，睡到 18:00 開抓；18:17／18:47 兩場收盤後備援（preflight 偵測到當天已發布會自動跳過，不重跑） | 交易日 18:00 起，抓不到就每 10 分鐘重試，最晚撐到 21:00 | 回補當日行情、日 K、重大訊息，同步 Supabase、對帳、備份原生資料、輸出並發布靜態網站 |
| `us-daily-snapshot.yml`（美股） | cron 定在 UTC 23:30 週一～五（此時 ET 還沒跨午夜，UTC 星期幾正好等於 ET 星期幾），進場後睡到 20:30 ET 才真正開抓，`TZ=America/New_York` 自動處理夏令／冬令 | 美股交易日 20:30 ET 後（約當地台北時間隔天 07:30～08:30，隨日光節約而異） | 先自動把資產持倉裡有、`us_watchlist` 裡沒有的美股 ticker 補進觀察清單（2026-09-03 起，見 `UsWatchlistStore.SyncFromHoldingsAsync`），再回補清單裡的美股 ticker（Yahoo Finance，一次拿近兩年歷史，2026-09-04 起改用），同步 Supabase、對帳；只有 `imports-us` 真有新快取時才委派既有 `publish-only=true` 輸出資產 K 線與名冊 |
| `revenue.yml`（月營收） | cron，公告期密集、之後每天一次 | 每月 1～10 日台北 09:20／11:20／13:20／15:20／17:20／19:20／21:20（每 2 小時一次）；11～31 日台北 19:20（每天一次） | 抓上市櫃公司月營收公告，只寫資料庫，不重新發佈靜態網站 |
| `restore-test.yml`（備份還原測試） | cron，每週一次 | 週日台北 08:00 | 把最新備份實際還原進一個拋棄式 Postgres 容器，驗證備份不只是「看起來像 SQL」 |

（歷史脈絡見 [版本紀錄.md](版本紀錄.md)；踩過的排程坑見下方「踩過並且已經解掉的坑」一節。）

## 最新已發布版本

本次「Podcast／股癌 UI 正式頁面」已以 `main` commit `0c820315767b5b5da629b16a52c470d6a4a09cc4` 推送，
並由 [`daily-snapshot.yml` publish-only run 33892822593](https://github.com/qwe953751/Investment/actions/runs/33892822593)
完成靜態輸出與 GitHub Pages 發布。Pages manifest 版本為 `1788537783`、最新交易日為 `2026/09/04`；
公開 manifest 與 `site.js` 在 CDN 延遲後已驗證為新版本。最高權限登入後，從正式網站的筆記頁切到
`?view=notes&notesTab=gooaye` 可查看附件確認的研究分析與 Podcast 來源版面；多筆匯入目前仍是展示操作，
尚未接正式 Gemini／API／資料庫。

本次「美股行情源切換成 Yahoo Finance；驗證持倉 K 線 MA240 算得出來」已推上 `main`
（`04a2084e`，含先前未推的 `bf326d17` 筆記 #48／#38／#49）並重新發佈 GitHub Pages。
`us-daily-snapshot.yml` Action `33788090134` 成功（13 檔美股全成功、寫入 503 個
交易日），`daily-snapshot.yml`（publish-only）Action `33788540715` 成功，快照版本
`1788458871`，最新交易日 2026/09/03。已在正式站驗證 AAPL／META／SOXQ／VT 的
65 根顯示棒全部帶出 ma240，SPCX 因 2026-06-12 才 IPO（僅 58 個交易日）正確地沒有
ma60／ma240。確切的提交與驗證細節記在 [版本紀錄.md](版本紀錄.md) 最上方。

目前 355 個測試全綠（本機 .NET 10 實跑），建置 0 警告 0 錯誤。

## 已經能做的事

### 資料收集

- [x] 證交所與櫃買中心的**每日收盤行情**逐日下載並快取（`backfill`）
- [x] 成交值**只計一般交易**：另外抓七份報表逐檔扣除零股、盤後定價、鉅額交易
- [x] 非交易日會被記錄，不會反覆重打同一個空日期
- [x] 用**官方休市日曆**分辨「休市」與「還沒公布」，且只用於降級（只能把紅燈變綠，不能阻止抓取）
- [x] 已下載的日期會略過，指令可以重複執行、中斷後續跑
- [x] `schemaVersion` 記錄成交值定義版本，定義改了舊檔自動重下
- [x] 盤後快照保存加權／櫃買指數與漲跌幅；舊快照可由 `backfill` 只補抓指數
- [x] 盤後快照保存日 K 開高低收；舊快照可由 `backfill-bars` 只補抓價格欄位
- [x] 每日回補同時抓 ETF（2026-09-01 起）；更早的舊快照可由 `backfill-etfs` 單獨補歷史，
  盤後排程有 `etf-backfill-days` 手動輸入可以觸發，預設 `0` 不跑
- [x] ETF 歷史已補到 **2025-06-13 起連續 300 個交易日**（每日約 355 檔），
  0050 這類老 ETF 的 K 線畫得出來了（2026-09-02 補，筆記 #47）
- [x] 盤中每 2 分鐘從 MIS 抓一輪報價寫進 Supabase（`intraday --loop`），輪距對齊時鐘
- [x] 盤中快照保存加權／櫃買指數與漲跌幅，`intraday_latest` 對外提供唯讀欄位
- [x] 盤中快照保存當日 K 的開、高、低，與加權／櫃買在同一輪更新
- [x] 盤中明細留到下一個有效交易日成功寫入才刪除；休市或失敗不清空
- [x] 盤中量能曲線每輪寫一列，這張表不會被 `sync` 刪掉
- [x] **月營收**：最新一期走兩支 OpenAPI，回補歷史走公開資訊觀測站的逐月報表（`revenue`）
- [x] 美股觀察清單（`us_watchlist`）自動同步：`backfill-us` 執行前先把資產持倉裡有、清單裡
  還沒有的美股 ticker 自動補進去，不用再手動去 Supabase Studio 新增（只新增，不動使用者
  手動停用過的既有列）
- [x] 美股回補資料源改用 Yahoo Finance（`query1.finance.yahoo.com` 公開 chart API，不用
  key、無公佈配額），取代原本卡在 25 次/日、近 100 個交易日封頂的 Alpha Vantage 免費方案；
  單次呼叫拿近兩年（約 500 個交易日）歷史，一輪就能補滿整份觀察清單，不用再分好幾天等配額
  （2026-09-04 切換，`AlphaVantageDailyQuoteClient` 保留在程式碼中作備援，未接線）

### 計算

- [x] 平均每日成交值、成交值增減率、市場成交比、排名變化、日漲跌與當週漲跌
- [x] 兩種排行模式：成交熱度（2N 天）、資金加速（3N 天）
- [x] 觀察期間 前一交易日 / 5 / 10 / 20 / 60 日
- [x] 基準交易日可往回選，該日之後的行情完全不列入計算
- [x] 資料不足時明確顯示還差幾個交易日，不給錯的數字
- [x] 前期平均為 0 時顯示 `—` 並排在最後，不當成 0%
- [x] **月營收摘要**：YOY、MOM、創幾個月新高，全部由自己的營收歷史算出，不抄報表算好的欄位
- [x] 「上個月」只認上個月，沒公告就顯示 `—`，程式裡沒有往回找上上個月的路徑
- [x] 公告期（每月 1～10 日）除了 OpenAPI 一定會再走一次公開資訊觀測站補上個月那一格，
  因為 OpenAPI 是整批出表的、看不到早報的那幾十家（2026-09-02 修，筆記 #47）
- [x] **除權息交易日的漲跌基準改用除權息參考價**，同時對得起券商 App／新聞與同一列的還原日 K
- [x] 公式全部有單元測試釘住（49 個）
- [x] **盤中全市場預估成交額改用校準過的日內量能曲線**：分母不再是線性時間比例，改用
  `IntradayTurnoverCalibration` 依 2 分鐘格對過去交易日的官方成交總額回推出的 f(t)（中位數，
  樣本不足退回寫死的 9 點 fallback 曲線），13:30 後夾住不外推；早盤（09:15）的高估倍數從
  4.75× 降到接近 1×（筆記 #42，2026-09-05）
- [x] **「資金加速」排行改用收縮量比 `(本期+k)/(基準+k)`**，取代原本的絕對差／未收縮量比，
  `k = 0.25 × 全市場基準中位數`：大型股倍數幾乎不變、迷你分母股的倍數被壓回 1 附近；
  流動性門檻同時從「過去平常量」搬到「當期成交值」，不再誤殺平常沒量、今天爆量的個股；
  `rankChange` 只在前期名次前 200 名內才顯示。唯一定義處在 C# `AccelerationRules`，
  `site.js` 的 `rankRows()`（盤後）與 `loadIntraday()`（盤中）收斂成同一組共用函式，
  係數一律讀 `manifest.acceleration`（筆記 #10，2026-09-05，係數以 286 個交易日、
  約 55 萬列正式歷史資料回測校準）

### 畫面

- [x] Blazor Interactive Server 互動排行頁（本機 `/trading-value-ranking`）
- [x] 靜態網站輸出，所有篩選組合預先算好（`export`）
- [x] **Podcast／股癌 UI 與本機來源管理**：保留「我的筆記」原設計；正式筆記頁新增「股癌」子頁籤，分成「研究分析／Podcast 來源」，來源輸入只需時間、集數與 Gemini Notebook 逐字稿分析；已移除硬編碼示意資料，來源可在目前瀏覽器本機新增、編輯、移除，分析頁無資料時顯示空狀態；尚未接正式 Gemini／API／資料庫
- [x] 盤中／盤後／自訂三個檢視切換
- [x] 盤中／盤後摘要顯示加權與櫃買指數及漲跌幅
- [x] 市場、成交門檻（含自行輸入任意金額）、排序都在前端就地處理
- [x] 交易限制標記：處置（紅）、全額交割（紫），股名左側直排、欄寬固定保留
- [x] 盤中的交易限制標記讀即時的 `market_flags` 表，不跟盤後共用每天只重抓一次的 `manifest.json`
- [x] 鎖定個股：顯示完整名次（不受只顯示前 100 名限制），存在瀏覽器裡
- [x] 鎖定個股點一下跳到它在排行榜裡的那一列，超過前 100 名沒有列可跳
- [x] 市場、股票名稱兩欄不開放排序（欄位本身不適合排序，其餘欄位維持可排序）
- [x] 每個標籤與欄位標題都有說明泡泡，兩邊文字刻意一模一樣
- [x] 表頭與內容一律置中，對齊不靠欄位位置決定
- [x] 檢查更新按鈕：重讀 `manifest.json`，版本有變就重載
- [x] **記住上次選的選項**：07:00–18:00 之間記憶，跨過 18:00 回到預設
- [x] 觀察期間預設值兩邊不同：盤後前一交易日、盤中 5 日
- [x] **月營收兩欄**：營收增減（YOY／MOM 上下兩行，以 YOY 排序）、
  創高月數（創高 `N` / `N+`、沒創高紅色 `✕`、那一期還沒公告 `—`，三種狀態各自不同）
- [x] 營收由瀏覽器直接讀 Supabase 的 `revenue_latest`，不進靜態快照（公告期內整天都在變）
- [x] 點營收儲存格開啟浮動彈窗：上層 20 個月圖表、下層最近 5 個月列表，高度比 3:2
- [x] `db/009_revenue_history.sql` 已套用；`revenue` Action 已寫入 39,448 列、1,976 檔，公開站已完成真實資料驗收
- [x] 前端再比對一次「上個月」，跨月當下資料庫還沒重算完也不會顯示錯的月份
- [x] 點擊標的名稱開啟最近三個月向前還原權息日 K 浮動視窗；不插入表格列、不改動原排行版面
- [x] 日 K 顯示 MA5／MA10／MA20／MA60／MA240，使用有效交易日收盤價
- [x] K 棒以上漲紅、下跌綠、平盤灰呈現，漲跌以收盤價相對前一交易日收盤判斷
- [x] 均線色票與常用 APP 一致：MA5 藍、MA10 薄荷綠、MA20 橘、MA60 粉、MA240 紫
- [x] MA240 距離近期價格太遠時標示圖外且不納入 Y 軸，避免三個月 K 棒被壓扁
- [x] 漲跌幅（日／週）與營收增減（YOY／MOM）共用上下層排版，上層 16px、下層 14px；排序固定使用日漲跌與 YOY
- [x] 盤中／盤後／自訂頁移除獨立市場欄，改在股票代號右側顯示「市／櫃」標記；市場篩選仍保留
- [x] 盤中指數與展開中的標的日 K 使用同一個 manifest 更新間隔同步重讀（目前 2 分鐘）
- [x] **自訂資料瀏覽**：單一交易日的完整上市櫃資料，不設預設排行，預設依代號排列
- [x] 自訂頁可選日期、設定獨立的單日成交值下限，顯示收盤價、營收增減（YOY／MOM）與成交值；點擊儲存格查看營收彈窗
- [x] 自訂頁每頁固定 100 檔，可用頁碼、上一頁與下一頁瀏覽全部符合條件的個股
- [x] 頁首「裝置」「異常通知」只有最高權限看得到，監控者／訪客即使有異常也看不到細節
- [x] 監控者權限的族群列表只顯示一個大族群（預設「伺服器」），不顯示搜尋框與篩選按鈕；
  從盤中／盤後／催化事件點某個族群跳轉過來時，族群列表會自動切成該族群所屬的大族群並選中它

### 資產

- [x] 資產持倉新增／批次編輯只允許代號、庫存數量、總成本；名稱、市值與未實現損益依名冊及最新有效行情自動重算
- [x] 資產 Dashboard 顯示每日資產總值折線圖；正式 Supabase 以使用者與日期保存一筆完整快照，缺行情或匯率時不寫入不完整數值
- [x] 帳戶明細頁也有自己的資產變化折線圖，跟 Dashboard 版共用同一份畫圖程式碼；資料各自獨立存在新表
  `asset_account_value_snapshots`（`db/037_asset_account_value_snapshots.sql`），套用前顯示「尚未啟用」，不假裝有資料
- [x] 資產 OCR 支援台股長表與美股 Positions 原圖／手機重壓縮圖；解析結果仍需人工逐項核對後才可寫入持倉
- [x] D+ AI OCR Phase 1 核心已建立：Claude／Codex CLI Adapter、Agent 間額度 fallback、雙 Pass
  checkpoint、`ocr-poc`，以及「Worker 離線／無已登入 Agent／雙額度不足時改走 Tesseract」的
  決策政策、`OcrExecutionCoordinator` 與單元測試；尚未執行真實 CLI／Golden Set，也未接正式網站或持倉
- [x] 出入金紀錄可直接就地編輯（原本打錯字只能刪除整筆重新輸入）；同一時間只有一列在編輯中
- [x] 右上角淺色／深色兩段外觀切換（2026-09-03 拿掉「跟隨系統」，兩顆按鈕互相獨立、直接指定，
  不經過系統偏好解讀），預設淺色、記在瀏覽器 `localStorage`
- [x] 資產頁台股持倉的 K 線彈窗與市值跟著盤中／盤後正確交接：開盤時用盤中即時棒，收盤到隔天
  18:00 官方盤後資料真的發佈之前繼續沿用當天最後一輪盤中資料，資料一發佈就換成官方收盤；
  判斷依據是「asset-catalog.json 的日期是不是今天」，不是單純看時鐘。美股帳戶沒有盤中概念，
  行為不變
- [x] 未實現損益一律顯示「金額(%數)」，不寫 +/− 符號，用既有紅漲綠跌色塊表達正負；美股帳戶
  的雙幣別百分比只在台幣主行顯示一次，不重複顯示
- [x] K 線查價資訊列精簡成「日期／現價(盤中)或收盤(盤後)＋漲跌幅／量」，個股與大盤指數兩條
  K 線都套用同一套規則
- [x] 登入／登出後「裝置」「異常通知」等最高權限限定的頁首小控件會重新判斷可見性，不會停留在
  切換前的狀態
- [x] 資產截圖 OCR 的代號→名稱覆蓋改成先跟既有 OCR 名稱做 Levenshtein 交叉比對才覆蓋，
  比對不過就清空代號、不寫入看起來合理但其實是別檔公司的名字（2026-09-03 修，筆記 #38）
- [x] 資產截圖 OCR 遇到頂部沒有多餘 App／狀態列 UI 可裁的截圖（表格從最上面就開始）時，
  會在同一個 10 秒預算內自動重跑一次不裁頂部的版本（2026-09-03 修，筆記 #38；深色版兩行
  一列版面與 Android 截圖辨識品質仍是已知殘留限制，見版本紀錄）
- [x] 資產持倉表格全部 8 欄（含代號）都可點擊排序，不再只有代號與漲跌幅；數字欄缺值固定
  沉底、文字欄用跟代號一致的排序規則（2026-09-03 新增，筆記 #49）
- [x] 美股持倉 K 線的年線（MA240）驗證跑得出來：改用 Yahoo Finance 回補後，實際持倉
  （META、AAPL、MSFT 等）本機都有 502～503 個交易日歷史，MA240 正常算出並隨畫面上的
  三個月窗格顯示；2026-06-12 才 IPO 的 SPCX 只有 58 天可用，MA60／MA240 依規則留空，
  屬於股票本身歷史不夠、不是程式錯誤（2026-09-04 驗證）

### 自動化

- [x] 每個交易日自動「回補 → 同步 → 對帳 → 備份 → 匯出 → 發佈 → 心跳」
- [x] 排程提早卡住 runner，再由程式自己等到 18:00
- [x] 抓不到今天的行情就每 10 分鐘重試，最晚撐到 21:00
- [x] 各步驟用 `!cancelled()` 彼此不互相拖累，前面紅了後面照跑
- [x] `data` 分支推送前檢查有沒有檔案被刪除，有的話直接中止
- [x] 心跳用 `always()`，前面炸掉也會記錄
- [x] 失敗時在 job summary 留下排查線索

### 資料保護

- [x] 每天 `verify` 逐日比對 `imports` 與 Supabase 的檔數、成交值總和、成交股數總和；
  `sync` 用**同一組條件**挑要重送的日期，所以對帳說不符的日期重跑 `sync` 一定會被修正
- [x] 每天備份 Supabase 的**原生資料**（行情表只備份結構，不備份內容）
- [x] 備份前先查目錄：有表指向被排除內容的表卻自己沒被排除時直接中止，
  不讓孤兒外鍵混進備份（那種備份要到下一次還原測試才會被發現）
- [x] GFS 輪替：最近 5 份、每週 4 份、每月 6 份，共 15 份約半年
- [x] 每週日把最新備份**真的還原一次**到臨時容器，並檢查行情表是不是空的
- [x] `schema_migrations` 比對 `db/` 檔名，漏貼哪一份會直接印出來
- [x] 族群分類（F:J 樹、概念股）讀 Google Sheet 失敗時退回 Supabase 備援快取
  （`db/036_topic_sheet_cache.sql`，已於 2026-09-02 套用正式 Supabase）；Sheet 讀得到時永遠不看快取，
  快取只存解析後的原始資料，套用的仍是當下最新的結構調整／補分類／使用者編輯規則

## 踩過並且已經解掉的坑

這一節是給另一台裝置的：這些問題**已經修好了**，
但如果之後看到類似症狀，先回頭看這裡，不要重新debug一遍。

| 症狀 | 真正的原因 | 怎麼解的 |
|---|---|---|
| 排程說 18:00 卻 18:31 才動 | GitHub cron 是盡力而為，實測誤點 31（盤後）與 88（盤中）分鐘 | 提早排在非整點非半點卡住 runner，程式自己睡到目標時間 |
| 盤後整天沒更新，`imports/` 停在上一個交易日，Actions 看起來也「有跑而且成功」（2026-08-31） | 當天 cron 一發都沒送到，補位的 18:00 鬧鐘又只看 run 的 `conclusion`。`publish-only=true` 的純發布會跳過「回補行情」但 run 一樣 `success`，白天用 AI agent 發布過網站就會讓鬧鐘判定「今天已經收過盤後」 | 鬧鐘改看**步驟層級**：逐一翻開當天的 run，確認「回補行情」這一步真的 `success` 過。**run 成功不等於資料進來了**，判斷完成度要看做事的那一步，不是 run 的結論 |
| 國定假日整條線紅燈 | 休市與「還沒公布」從 API 看起來一模一樣，都是空的 | 接官方休市日曆，**只用於降級** |
| 休市日曆把交易日判成休市 | 那份清單混著「春節前最後交易日」這種**照常開市**的日子 | 過濾掉名稱含「交易日」的列；拿 320 天快取交叉驗證過，零誤判 |
| 成交值跟玩股網差很多 | 官方成交金額含零股、盤後定價、鉅額交易 | 另抓七份報表逐檔扣除；台積電單日差過 59 億，足以改名次 |
| 盤中收集器整天沒寫入卻不報錯 | HTTP timeout 太短，個股清單抓不到就整場停擺 | 加長 timeout 並補上清單備援 |
| 手機看到的還是舊資料 | Pages 快取十分鐘且無法改標頭 | 網址帶 `?v=發佈時間`，`manifest.json` 用 `no-store` 讀 |
| 匯出的整包撐不住 | 資料檔連顯示文字都先算好，單檔快 1 MB | 只留數字、顯示格式交給前端，單檔剩四分之一 |
| `pg_dump: server version mismatch` | runner 內建 16，Supabase 是 17.6，版本不合直接 exit 1 | 掛上 pgdg 套件庫裝 17 |
| 裝了 17 還是回報 16.14 | `/usr/bin/pg_dump` 是 pg_wrapper，runner 上有 16 的預設 cluster 就照那個挑 | 直接把 `/usr/lib/postgresql/17/bin/pg_dump` 交給備份腳本（`PG_DUMP`） |
| 同步／對帳／心跳全部 `28P01` | GitHub Secret 裡的資料庫密碼過期 | 重設 `SUPABASE_DB_URL` |
| 盤中排行每輪換一批人，全市場累計金額還會**倒退**（159 億～940 億亂跳） | MIS 的 `z` 不是「最後成交價」，是「這次快照那一瞬間有沒有成交」。實測台積電連查六次 `z` 全是 `-`，但 `v` 一直在跳。只看 `z`／`pz` 的結果是 1972 檔裡 1800 多檔現價 null、成交金額算成 0 | 缺 `z`／`pz` 時依序退到買賣中價、高低中價、開盤、昨收。修完那輪：成交價 145 檔、買賣中價 1830 檔、有量卻沒價 0 檔 |
| 排行榜最後幾百檔的營收全是 `—`，資料庫裡明明有 | PostgREST 一次最多只回 **1000 列**，超過的直接截掉而且回 200 不是錯誤。`revenue_latest` 有 1976 列，後面 976 檔整批消失卻沒有任何徵兆 | 加上 `fetchAllRows()`，用 `Range` 標頭一頁一頁拿到尾。**同一支也修掉 `intraday_latest`**：那邊的市場成交比分母是全市場加總，少拿一半分母就偏小、每一檔的比例全部偏高 |
| 從本機發佈，線上反而**倒退**兩個交易日（2026-08-19） | 行情快取住在 `data` 分支，不在 main。Mac 離家幾天沒開機，本機 `data/imports/` 停在 08/14，CI 早就把 08/17、08/18 收進去了。本機 `export` 只看本機那份，於是產出一份少兩天的快照，`publish-gh-pages.sh` 又是整個分支覆蓋，一推就把 CI 的新快照蓋掉 | 本機發佈前先 `git fetch origin data` 把缺的交易日補回 `data/imports/` 再 `export`。這一次是從 `origin/data` 取回兩檔、重跑 export、重新發佈。**發佈完一定要拉 `manifest.json` 對 `latestTradingDate`**，光看「已發佈」不算數 |
| 說好 5 分鐘一輪，實測每輪都是 5 分 12 秒到 6 分 15 秒，而且每天飄的量不一樣（2026-08-19） | 迴圈是「跑完再睡 5 分鐘」，於是每一輪的工作時間（實測 12～75 秒）全部疊進輪距裡。時刻也因此一路往後漂，同一個時刻在不同天對不齊，`intraday_curve` 的格子跟著糊掉 | 改成對齊時鐘：`CollectionSchedule.NextRound()` 把時間切成間隔的格子、回傳下一格，睡到那個時刻為止。輪距就真的等於間隔，時刻固定落在 09:00、09:02、09:04……某一輪跑過頭就跳過那一格，不補跑也不會兩輪擠在一起 |
| 盤中偶爾整輪沒寫入，重打卻又正常（2026-08-19） | 全市場拆成 14 批請求，任何一批逾時或收到被截斷的 JSON 就丟例外，整輪 1974 檔全部作廢。實測 09:48 與 09:50 連兩輪陣亡，但同一時間單獨手打 150 檔批次三次都正常（0.3～1.9 秒）——是傳輸層的隨機抖動，不是 MIS 掛掉 | 單批就地重試三次（`MisIntradayClient.MaxAttempts`），每次自己有 15 秒上限，不再吃共用 HttpClient 那個為盤後大報表設的 60 秒。整輪另外有等於一個間隔的硬預算，超時就讓這輪失敗、直接跳下一格。**重試失敗仍然整輪作廢**：少一輪可以接受，寫進少了 150 檔的殘缺快照會讓合計、成交比、量能曲線同時失真而且事後看不出來 |
| `export` 跑完顯示成功，`publish/site` 裡卻還是上一次的檔案（2026-08-19） | `dotnet run --project src/Invest.Web` 會把工作目錄切到**專案資料夾**，所以 `export publish/site` 這個相對路徑其實寫到 `src/Invest.Web/publish/site`；`publish-gh-pages.sh` 預設讀的是 repo 根目錄的 `publish/site`。兩邊對不上，而且兩支指令都回報成功 | 路徑一律給絕對的：`export "$(pwd)/publish/site"`，README 與開發環境文件都改成這樣。`publish-gh-pages.sh` 也把最新交易日印進「已發佈」那行——它推的是上次留在硬碟上的檔案而不是當下的資料庫，日期擺在眼前才看得出推錯。**發佈後一定要拉線上的 `manifest.json` 對 `version` 與 `schedule`**，跟本機產出的那份逐項比 |
| 除權息當天，表格顯示一根跌幅、同一列點開的 K 線卻是平的（2026-08-23） | 表格的漲跌拿**未還原**的原始收盤價算，K 線是**向前還原**的，除權息當天兩個基準本來就差一個還原因子 | 除權息交易日的基準改成 `基準收盤價 × 還原因子`，也就是官方的**除權息參考價**。交易所當天就是用它報漲跌，所以跟券商 App 一致；它同時是還原序列那天的基準，所以跟 K 線也一致。只有除權息當天會變，2026-08-21 的 1,957 列只動了 5 列 |
| 某個月的營收整批消失，畫面上只是少一欄，沒有任何錯誤（2026-08-23 review 發現） | `RevenueClient` 抓到例外就記一行 warning、回空的，於是「這個月還沒公告」與「連線壞掉／觀測站改版」在呼叫端長得一模一樣 | 失敗改成往外丟並中止整批；傳輸層抖動就地重試三次（沿用 `CorporateActionClient` 那套）；HTTP 404 是合法的「沒有這份報表」不重試。另外用回應大小分辨「空表」與「解不出來」——一頁有內容卻零列就是版面改了 |
| 某天寫到一半被中斷，之後永遠補不回來（2026-08-23 review 發現） | `DailyQuoteSyncStore` 只看「這個日期在不在資料庫裡」，殘缺的那天日期已經有了，所以每次都被跳過 | 改成比**每天的檔數**：本機比資料庫多就補寫。順手擋掉保留視窗外的日期，那些送上去也會被 `PruneAsync` 立刻刪掉 |
| 盤中排行把已經解禁的個股標成處置中（2026-08-18，3081） | 處置／全額交割標記放在 `manifest.json`，那份只有每天盤後 `export` 時（約 18:00）重抓一次，之後整個交易日的盤中畫面都共用同一份沒再更新過的快照；解禁常常發生在收盤後到隔天開盤之間 | 新增 `market_flags` 表（`db/005_market_flags.sql`），盤中 Action 開場第一輪整批重寫一次；盤中頁改讀這張表，不再跟盤後共用 manifest 那份 |
| 自走鏈明明在跑、下一棒也排好了，開盤後卻一個多小時沒人收集（2026-08-31） | `concurrency` 是**整個 run** 層級的鎖。`hop` 判斷自己撐不完整場、提早交棒收工後，同一個 run 裡的 `publish-manifest` 還要再等 CDN 五個半小時，run 就一直算 `in_progress`，排隊中的下一棒進不來 | `publish-manifest` 等 CDN 的迴圈每輪順便查一次同一個 run 裡的 `hop` 收工了沒，收工就跳出去把鎖讓給下一棒。**單一 job 的假時鐘模擬驗不出 run 層級的鎖**，這是 08-29 驗證漏掉它的原因 |
| `intraday.yml` 排 07:03 時，08-19～08-21 連三天收盤前幾分鐘的盤中資料收不到，job 顯示 `cancelled`、歷時 6:00:1x | GitHub 對單一 job 有六小時硬上限，時間到直接取消，`timeout-minutes` 設多大都沒用；07:03 排程實際約 07:30 才開跑，六小時上限剛好落在 13:30～13:34，正好把收盤吃掉 | 改排 07:33，上限推到 14:00 才過收盤；同時保留給排程本身誤點的餘裕（實測誤點 28～88 分鐘，07:33 誤點 87 分鐘仍趕得上 09:00 開盤前開跑）。後來這個排程又實測誤點到 6～13 小時甚至完全沒觸發，才改成現在的「自走鏈」設計，cron 降級為復原用的火種 |
| 深色模式上線後，筆記清單卡片變成淺色文字疊在白底上，幾乎看不清楚（2026-09-02） | 全站色票轉換成 CSS 變數時是用十六進位色碼（`#fff`／`#ffffff` 等）批次比對取代的；`.notes-list-item` 這處背景是用關鍵字寫的 `background: white;`，不在比對規則裡，沒被轉到，留下唯一一處背景仍是固定白色、文字卻已經是深色模式的淺色 | 另外查一輪關鍵字寫的顏色（`white`／`black` 等），跟十六進位色碼分開處理；改完全站關鍵字顏色只剩「固定深底＋固定白字」這種刻意不隨主題變的強調色塊（例如選取中的頁籤），不是漏網的表面色。**之後再擴充深色模式規則時，要同時查十六進位與關鍵字兩種寫法，只查一種會漏** |
| 資產持倉表格某一欄看起來沒跟同一列其他欄位對齊，量測發現矮了 5px（2026-09-02） | `.asset-holding-price-change` 這個 class 直接掛在 `<td>` 上，用 `display: grid` 疊漲跌幅與盤中／盤後兩行文字；`display: grid` 會讓這顆儲存格脫離表格的儲存格版面配置，不再跟同一列其他 `<td>` 共用列高 | **不要把 `display: grid`／`flex` 直接設在 `<td>`／`<th>` 上**，要疊多行內容就在裡面包一層 `<div>`／讓兩個行內子元素各自 `display: block`，儲存格本身維持預設的 `table-cell`，才會跟同列其他欄位等高。這顆坑值得記住：以後任何一格要疊多行文字，先檢查 grid/flex 有沒有不小心設到 `<td>` 本身 |
| 筆記 #46 的 K 棒顏色「修好了」還是被使用者打回票，說 09/01 明明該是紅棒（2026-09-02） | 上一輪把規則改成「已收盤棒比昨收、盤中即時棒比開盤」兩套規則並存，只照字面解讀了使用者附圖裡「盤中」兩個字，沒注意到使用者附的另一張圖是**已經收盤**的日期，而使用者要的其實是「所有棒子都比自己的開盤價」，沒有已收盤／盤中之分 | 這個坑不在程式碼，在**理解需求時只看到局部就動手改**。使用者給的是同一個功能的兩次反饋（筆記＋事後驗收），第二次反饋出現「請檢查所有個股的 K 棒邏輯」這種要求全面檢查的字眼時，代表第一次的修法可能只對了一半，要重新從頭想這條規則到底該怎麼定義，不是在舊邏輯上再加一個特例分支 |
| 差點依照自己上一輪對「外觀切換」的記憶（系統／深色二選一）繼續改，實際上按鈕已經變成別的樣子（2026-09-02～09-03） | 使用者換了台裝置、用另一個 AI agent 做了一輪修改，把上一輪拿掉的「淺色」按鈕加了回來（系統＋深色在深色 OS 上會讓使用者永遠回不去淺色，是真的 bug）。**上一輪的決定跟這次要接手的現況已經不是同一回事**，只憑對話記憶接手會在錯的基礎上繼續改 | 多裝置／多 AI agent 交替開發時，接手前一定要先 `git fetch` 並實際讀一次現有程式碼，不能只靠自己記得「上次改成怎樣」。這次剛好也順便解出另一個更安全的做法（拿掉「系統」只留淺色／深色兩顆互相獨立的按鈕），比兩輪之前任何一版都不會卡死 |
| 登入最高權限又登出後，「裝置」「通知」兩顆最高權限才該看到的按鈕還留在畫面上（2026-09-03） | `wireDevicePresence()` 只在 `SITE_ACCESS === 'admin'` 那條路把 `hidden` 設成 `false`，不是最高權限時直接早退，從來不會主動設回 `true`；登入／登出共用的收尾函式 `afterAccessChange()` 也沒有再呼叫一次它或 `refreshAlerts()`，於是這兩顆按鈕的可見狀態只在「頁面第一次載入」那個時間點被判斷過，之後權限中途升降都不會再檢查 | 任何「只在頁面初始化時判斷一次 `SITE_ACCESS` 就設定 `hidden`」的寫法，都要同時想清楚「權限中途變動時誰會再呼叫這段邏輯」——這個站台權限可以透過登入／登出在同一頁切換，不是只有換網址、重新整理才會變。`afterAccessChange()` 是目前唯一的「權限剛變」收尾點，之後新增任何 tier 相關的頁首小控件，記得一併掛進去，並且把「不是這個權限」那個分支寫成明確關閉，不要靠早退 |
| 「每日排行快照」「月營收」兩條排程同時紅燈，前者卡在最後「心跳與狀態」步驟、後者一啟動就整批失敗（2026-09-04） | 另一台裝置／AI agent 當天稍早推了 `db/038_topic_edits_rename_action.sql`（族群改名功能）進 `main`，但只套用到 repo，沒實際套用到 Supabase。`dotnet run -- status`／`revenue` 兩個指令開頭都會檢查 `db/*.sql` 是否都已登記進 `schema_migrations`，發現 038 沒登記就直接 `exit 1`——`revenue` 是**還沒開始抓資料就整批中止**，`每日排行快照` 則是主體（回補行情／輸出網站／發佈）都已經成功，只有最後寫 `STATUS.md` 那一步跟著炸掉，兩者症狀不同但根因相同 | 用 Management API 補套用 `038_topic_edits_rename_action.sql`（見 `reference_supabase_ddl.md`），確認 `schema_migrations` 登記後手動 `gh workflow run` 重跑兩條，皆全綠；月營收從 100 檔補到 219 檔（今天公告期間累積的公司都補回來了）。**多裝置／多 AI agent 交替開發時，`db/*.sql` 是本機 Management API 套用、跟 `git push` 完全脫鉤的兩件事**，任何一台裝置新增 migration 檔後，若沒有人記得手動套用，兩條每天都會跑的排程會在下一次執行時全部卡住；之後接手看到「所有排程一起紅」，先查 `schema_migrations` 有沒有漏登記的檔案，不用逐一重新 debug |
