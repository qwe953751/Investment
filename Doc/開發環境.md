# 開發環境

在**另一台裝置**上要能編輯這個專案，需要裝什麼、要什麼權限，
以及**主力 Mac 上實際裝了什麼**——給接手的人或 AI agent（CodeX、Copilot⋯⋯）逐項對照自己的機器，缺什麼補什麼。

- 這個專案在做什麼 → [專案計劃書.md](專案計劃書.md)
- 已經做完什麼 → [完成進度.md](完成進度.md)
- 功能與指令 → [README.md](../README.md)
- 最近發布與跨裝置接手 → [版本紀錄.md](版本紀錄.md)
- 還沒做完的事 → [TODO.md](../TODO.md)

主力機盤點時間：**2026-08-23**。版本會漂，對照時以「大版本相符」為準，不必分毫不差。

---

## 先跑這段，再往下看

複製貼上，輸出拿去對下一節的表：

```bash
echo "== 作業系統 =="       ; uname -sm; sw_vers 2>/dev/null | tail -2
echo "== .NET =="           ; dotnet --list-sdks; dotnet --version
echo "== Git =="            ; git --version; git remote -v
echo "== GitHub CLI =="     ; gh --version | head -1; gh auth status
echo "== python3 =="        ; python3 --version
echo "== jq / curl =="      ; jq --version; curl --version | head -1
echo "== pg_dump =="        ; command -v pg_dump || ls -d /opt/homebrew/Cellar/libpq/*/bin/pg_dump 2>/dev/null || echo "沒有 pg_dump"
echo "== 憑證（只看有沒有，不印值）=="
[ -n "$SUPABASE_DB_URL" ]      && echo "SUPABASE_DB_URL ✅"      || echo "SUPABASE_DB_URL ❌ 未設"
[ -n "$SUPABASE_ACCESS_TOKEN" ] && echo "SUPABASE_ACCESS_TOKEN ✅" || echo "SUPABASE_ACCESS_TOKEN ❌ 未設"
echo "== 能不能建置 =="     ; dotnet build -v q && dotnet test -v q
```

最後一行過了，就代表**改程式碼、推上去**這條路是通的。其餘都是選配。

---

## 裝置分工

**這台 Mac 是主力機**，其他裝置是輔助。原因不是偏好，是有幾件事只在這台做過：

| 只在 Mac 上有的東西 | 為什麼重要 |
|---|---|
| `~/.zshrc` 裡的 `SUPABASE_DB_URL` 與 `SUPABASE_ACCESS_TOKEN` | 沒有這兩個就不能同步、備份、貼資料表定義 |
| GitHub 的 SSH key | `origin` 用的是 `git@github.com:` |
| 已經回補好的 `data/imports/`（328 天，約 20 MB） | 新裝置可以從 `data` 分支取，不必重抓 |

**好消息是：日常運作完全不需要任何裝置開機。**
每天的回補、同步、備份、發佈都在 GitHub Actions 上跑。
輔助裝置要做的只有「改程式碼」跟「看網站」。
所以接手時**先確認「是不是真的需要在本機跑」**：多數故障只要在 Actions 上重跑就解決了，
那條路完全不需要下面任何一個套件。

## 必裝（不裝就不能改程式碼）

| 套件 | 主力機的版本 | 安裝來源 / 路徑 | 誰在用 | 缺了會怎樣 |
|---|---|---|---|---|
| macOS | 26.1（25B78）、**arm64** | — | — | 專案本身跨平台，Linux/Windows 都行 |
| .NET SDK | **10.0.302**（host 10.0.10, osx-arm64） | 官方安裝檔，`/usr/local/share/dotnet`；<https://dotnet.microsoft.com/download> | 全部 `dotnet` 指令 | 完全不能建置，`net10.0` 不能用 8/9 跑 |
| Git | 2.39.5（Apple Git-154） | Xcode Command Line Tools / Windows 裝 Git for Windows | 版控、`data` 分支操作 | 不能 clone / push |
| GitHub CLI `gh` | 2.97.0 | `brew install gh` / `winget install GitHub.cli` | 看 Actions、觸發 workflow、讀 secret 清單 | 只是不方便，網頁可代替 |

repo 根目錄的 `global.json` 會選擇 .NET SDK **10.0.302**，並允許同一個 .NET 10 major/minor 下較新的 feature band。
這個設定只會在目前 repo 及其子目錄生效，不會替其他專案改 SDK；這個專案也因此不接受 .NET 8/9。

NuGet 套件（`dotnet restore` 會自己抓，不必手動裝）：
`Npgsql 10.0.3`、`xunit 2.9.3`、`Microsoft.NET.Test.Sdk 17.14.1`、`coverlet.collector 6.0.4`。
`dotnet test` 目前是 **113 個測試全過**。

驗證裝好了：

```bash
dotnet --version    # 要是 10.x；本 repo 的 global.json 會再限制 SDK 範圍
git --version
gh auth status      # 要顯示已登入 qwe953751
```

編輯器建議 **VS Code + C# Dev Kit**。用 Visual Studio、Rider 也可以，
專案是標準的單一 Solution（`Invest.sln`），沒有任何 IDE 專屬設定。

## 選配（什麼時候才需要裝）

這些**不裝也能改程式碼並推上去**，只有要在本機做特定事情時才需要：

| 套件 | 主力機的版本 | 安裝來源 / 路徑 | 只有做這件事才需要 |
|---|---|---|---|
| `libpq`（提供 `pg_dump`） | **18.6** | `brew install libpq`，`/opt/homebrew/Cellar/libpq/18.6/bin/` | 在本機備份 Supabase（`scripts/backup-supabase.sh`） |
| `python3` | 3.11.13 | Homebrew `python@3.11`，`/opt/homebrew/opt/python@3.11` | 備份腳本的 GFS 輪替、本機預覽靜態站 |
| `jq` | 1.7.1-apple | **macOS 內建**，不是 brew 裝的 | 把 `db/*.sql` 送進 Supabase Management API |
| `curl` | 8.7.1 | macOS 內建 | 同上 |
| Docker | **主力機沒裝** | Docker Desktop | 在本機試還原備份（週日的 workflow 已經自動做了） |
| VS Code | — | `/usr/local/bin/code` | 編輯器，換成任何 IDE 都行 |

**不需要 Node.js。** 前端只有一份手寫的 `site.js`，沒有 npm、沒有建置步驟。
主力機上另外還有 `gcc`、`python-tk@3.11`、`unar`、.NET SDK 9.0.101，那些是其他專案在用的，**不要跟著裝**。

### `libpq` 的兩個雷（都已經在腳本裡處理掉了）

1. **它是 keg-only**，`brew install libpq` **不會**把 `pg_dump` 放進 `PATH`。
   主力機跑 `which pg_dump` 是找不到的，但備份照樣能跑——
   [backup-supabase.sh:21](../scripts/backup-supabase.sh#L21) 會自己去 `/opt/homebrew/Cellar/libpq/*/bin/` 撈。
   要指定別的路徑就設環境變數 `PG_DUMP`。
2. **`pg_dump` 的版本不可以比伺服器舊。** Supabase 是 PostgreSQL **17.6**，
   主力機是 18.6（比較新，可以）。用 16 會直接 `exit 1`，不是警告。
   GitHub Actions 的 runner 內建 16，所以 workflow 裡有一步專門裝 17。

### Windows 的 PATH

Windows 若 `C:\Program Files\dotnet\dotnet.exe` 排在 PATH 前面，可能會先找到 .NET 8，
即使使用者帳號已安裝 .NET 10。新開 PowerShell 後執行：

```powershell
dotnet --version
```

結果必須是 `10.x`。若仍是 `8.x`，把使用者 PATH 的
`%LOCALAPPDATA%\Microsoft\dotnet` 放到目前 shell 的 PATH 最前面，或直接使用該目錄內的 `dotnet.exe`。
不要為了這個 repo 改掉整台機器的 Machine PATH；這個專案的 `global.json` 已經負責 SDK 選擇，
shell 只需要先找到能看見 .NET 10 SDK 的 host。

那台 Windows 的 PowerShell 7 與 Windows PowerShell 5.1 都已設定使用者 profile，
會將上述目錄放到 PATH 最前面；目前使用者的 execution policy 是 `RemoteSigned`，
而 `LocalMachine` 仍維持 `AllSigned`。新開任一種 PowerShell 後，`dotnet --version` 都應是 `10.x`。

若在另一台 Windows 需要同樣設定，請在該使用者自己的 PowerShell 執行：

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

## 缺了怎麼補

```bash
# macOS
brew install gh libpq            # jq、curl、python3 系統多半已經有
# .NET 10 SDK 走官方安裝檔：https://dotnet.microsoft.com/download

# Ubuntu / Debian
sudo apt-get install -y gh jq python3 postgresql-client-17
# postgresql-client-17 在預設套件庫可能找不到，要先掛 pgdg 套件庫

# Windows
winget install GitHub.cli Microsoft.DotNet.SDK.10 jqlang.jq
```

補完再跑一次最前面的驗證區塊。

## 環境變數

| 變數 | 給誰用 | 哪裡拿 | 沒有的話 |
|---|---|---|---|
| `SUPABASE_DB_URL` | `sync` / `verify` / `status` / `intraday` / `backfill-intraday-heat` / 備份腳本 | Supabase 專案 → Settings → Database → Connection string（寫入帳號 `invest_writer`） | 這幾個指令直接報錯，其他功能正常 |
| `SUPABASE_ACCESS_TOKEN` | 貼 `db/*.sql` 與受控的盤中熱絡回填 Management API | Supabase → Account → Access Tokens | 只影響建表與明確指定的回填，不影響日常 |
| `PG_DUMP` | 備份腳本 | 選配，指定 `pg_dump` 的絕對路徑 | 腳本自己找 |
| `INTRADAY_SOURCE` | 盤中收集器標記來源 | 選配，預設是機器名稱 | 用機器名稱 |

Mac 上這兩個放在 `~/.zshrc`：

```bash
export SUPABASE_DB_URL='[連線字串]'
export SUPABASE_ACCESS_TOKEN='[token]'
```

`~/.zshrc` 裡的東西**只有互動式 shell 讀得到**。非互動指令（很多 agent 預設如此）要用 `zsh -ic '...'`，
否則會拿不到變數而誤判成「沒設」。

**這兩個值不在 repo 裡，也永遠不要放進 repo。**
`appsettings.json` 裡那組 Supabase URL 與 anon key 是**故意公開的**：
anon key 只有 select 權限（見 `db/001_intraday.sql` 的 RLS），
而且它會被發佈到 gh-pages，任何人都看得到，藏起來沒有意義。

`SUPABASE_ACCESS_TOKEN` 約 **2026-09-15** 到期（TODO 6 在追）。

## 憑證與權限

### GitHub

| 項目 | 主力機的實際狀態 | 權限範圍 | 影響什麼 |
|---|---|---|---|
| SSH key | `~/.ssh/id_ed25519`，`origin` 是 `git@github.com:qwe953751/Investment.git` | 該帳號對 repo 的寫入權 | `git push`。不想設 key 就改用 HTTPS（見下） |
| `gh` 登入 | 帳號 `qwe953751`，OAuth token（`gho_`）存在 macOS keyring | scopes：**`repo`**、**`workflow`**、`read:org`、`gist` | `repo` 給讀寫程式碼與觸發 workflow；**`workflow` 是改 `.github/workflows/*.yml` 必須的**，少了它 push 會被 GitHub 直接拒絕 |
| Actions secret `SUPABASE_DB_URL` | 已設在 repo secrets | 只有 workflow 讀得到，人看不到值 | 每日回補、同步、對帳、備份、盤中收集 |
| Actions 內建 `GITHUB_TOKEN` | workflow 自帶 | `permissions: contents: write` | 推 `data` 與 `gh-pages` 分支 |
| GitHub Pages | 已開在 `gh-pages` 分支 | 公開 | <https://qwe953751.github.io/Investment/> |

**接手的機器要用 HTTPS 而不是 SSH：**

```bash
git remote set-url origin https://github.com/qwe953751/Investment.git
gh auth login          # 選 HTTPS，讓 gh 代管 git 憑證
```

若是用 PAT 而不是 `gh auth login`，fine-grained token 需要：
**Contents RW**（推程式碼）、**Actions RW**（觸發／重跑 workflow）、
**Metadata R**（必選）、Pull requests RW（只有要開 PR 才需要）。
classic token 則是 `repo` + `workflow` 兩個勾。

### Supabase

專案 `dehzxlxyfvtnylwgbmqy`，PostgreSQL 17.6。

| 憑證 | 放在哪 | 身分與權限 | 缺了會怎樣 | 到期 |
|---|---|---|---|---|
| `SUPABASE_DB_URL` | 主力機的 `~/.zshrc`；CI 在 repo secrets | 寫入帳號 **`invest_writer`**，可讀寫資料，**沒有 CREATE／DDL 權限**，也不是 `postgres` 的成員 | `sync`／`verify`／`status`／`intraday`／`backfill-intraday-heat`／`backup-supabase.sh` 直接報錯；其餘功能正常 | 無 |
| `SUPABASE_ACCESS_TOKEN` | 主力機的 `~/.zshrc` | Management API，以 `postgres` 身分執行，**足以做 DDL** | 影響「改資料表」（送 `db/*.sql`）與明確指定的 `backfill-intraday-heat --via-management-api`；不影響日常任何流程 | **約 2026-09-15**（TODO 6 在追） |
| Supabase anon key | `src/Invest.Web/appsettings.json` | **故意公開**，只有 select（RLS 見 `db/001_intraday.sql`） | — | 無 |

### 對外連線（公司網路可能擋）

程式會連這些主機。若在受管制的網路裡跑不起來，先確認不是被防火牆擋掉：

| 主機 | 用途 | 哪個指令會用到 |
|---|---|---|
| `www.twse.com.tw` | 上市收盤行情、非一般交易報表、處置公告 | `backfill` |
| `openapi.twse.com.tw` | 官方休市日曆、上市個股清單、變更交易 | `backfill`、`intraday` |
| `mis.twse.com.tw` | 盤中即時報價（請求要帶 `Referer`，否則被擋） | `intraday` |
| `www.tpex.org.tw` | 上櫃的以上全部 | `backfill`、`intraday` |
| `<專案>.supabase.co` | 前端讀盤中資料（anon key） | 瀏覽器 |
| Supabase PostgreSQL 直連 | 同步、對帳、備份、盤中寫入 | `sync`／`verify`／`status`／`intraday`／備份 |
| `api.supabase.com` | Management API（DDL／受控回填） | 送 `db/*.sql`；必要時回填既有盤中熱絡欄位 |
| `github.com`、`api.github.com` | 版控與 Actions | `git`、`gh` |
| `api.nuget.org` | 套件還原 | `dotnet restore` |

## 第一次在新裝置上開工

```bash
git clone git@github.com:qwe953751/Investment.git
cd Investment

dotnet build
dotnet test            # 目前應該是 113 個測試全過
```

到這裡就能改程式碼了。**下面幾步只有想在本機看到畫面才需要。**

取現成的行情快取（比重抓快非常多，重抓 300 天要好幾小時）：

```bash
git clone -b data git@github.com:qwe953751/Investment.git data-branch
mkdir -p data
cp -R data-branch/imports data/
```

或是自己回補（每次請求間隔 3 秒，70 個交易日約 15 分鐘）：

```bash
dotnet run --project src/Invest.Web -- backfill 300
```

啟動互動網頁（排行頁在 `/trading-value-ranking`，Terminal 會印出網址）：

```bash
dotnet run --project src/Invest.Web
```

匯出並在本機預覽靜態網站：

```bash
dotnet run --project src/Invest.Web -- export "$(pwd)/publish/site"
python3 -m http.server 5199 --directory publish/site
```

輸出目錄一定要給絕對路徑：`dotnet run --project` 會把工作目錄切到專案資料夾，
相對路徑會落在 `src/Invest.Web/publish/site`，跟 `scripts/publish-gh-pages.sh` 讀的位置對不上，
而且指令照樣回報成功。

`.claude/launch.json` 裡已經定義好這兩個（`invest-web` 用 5188、`static-site` 用 5199）。

## 指令一覽

```bash
dotnet run --project src/Invest.Web -- backfill [交易日數] [起始日期]
dotnet run --project src/Invest.Web -- backfill-bars [交易日數] [起始日期]
dotnet run --project src/Invest.Web -- export   [輸出目錄]
dotnet run --project src/Invest.Web -- intraday [--loop]
dotnet run --project src/Invest.Web -- backfill-intraday-heat [--via-management-api]
dotnet run --project src/Invest.Web -- sync     [保留交易日數]
dotnet run --project src/Invest.Web -- verify
dotnet run --project src/Invest.Web -- status   [來源] [輸出檔]
dotnet run --project src/Invest.Web -- curve

scripts/publish-gh-pages.sh publish/site
scripts/backup-supabase.sh [快照目錄]
```

盤中日 K 的當日開、高、低欄位由 `db/008_intraday_kline.sql` 提供；套用新 migration 後，
盤中頁會用 manifest 傳來的收集間隔，同步更新加權／櫃買指數與展開中的標的 K 棒。

族群頁的「盤中」觀察期由 `db/013_intraday_topic_heat.sql` 提供。先在 Supabase SQL Editor
套用這份 migration，再執行 `intraday --loop`；每輪 MIS 原始快照成功寫入後，C# 會以同一輪的
累計成交值與即時漲跌、沿用 `TopicHeatCalculator` 算出族群結果，前端只讀
`intraday_topic_heat_latest`，不在瀏覽器重算。若 migration 尚未套用，盤中族群選項會誠實顯示
沒有資料；不應以盤後資料替代。

需要 `SUPABASE_DB_URL` 的是：`intraday`、`backfill-intraday-heat`、`sync`、`verify`、`status`、`backup-supabase.sh`。
若公司網路不能直連 PostgreSQL，`backfill-intraday-heat --via-management-api` 可用公開唯讀快照與
`SUPABASE_ACCESS_TOKEN` 回填 migration 前留下的最新盤中熱絡欄位；一般盤中收集不得使用這個選項。

## 分支規則

| 分支 | 放什麼 | 規則 |
|---|---|---|
| `main` | 原始碼 | 日常都在這裡，沒有在用 PR 流程 |
| `data` | `imports/`（行情快取）、`snapshots/`（備份）、`STATUS.md` | **只增不減。** 要刪任何交易日都得先確認 |
| `gh-pages` | 靜態網站 | 每次發佈都是全新的 orphan commit 覆蓋整支，不留歷史 |

`data` 與 `gh-pages` 都由 GitHub Actions 自己推，**平常不要手動動它們**。
`gh-pages` 一份快照就一百多 MB，不要 clone 它。

`origin` 上還留著 8 支已經合併完、沒清掉的舊分支（`agent/fix-intraday-collection`
與 7 支 `codex/*`），可以忽略；清理列在 TODO 10。

## 各裝置做得到什麼

| 裝置 | 改程式碼 | 本機跑網頁 | 本機備份 | 觸發／重跑 workflow | 看網站 |
|---|---|---|---|---|---|
| 主力 Mac | ✅ | ✅ | ✅ | ✅ | ✅ |
| 公司電腦 / 其他 Mac、Windows、Linux | ✅ 裝好 .NET 10 就行 | 需要行情快取 | 需要 `SUPABASE_DB_URL` + `pg_dump` ≥ 17 | 需要 GitHub 權限 | ✅ |
| iPhone / iPad | 只能用 GitHub 網頁編輯器改文字檔 | ❌ | ❌ | ✅ 用 Safari 開 github.com | ✅ |

**改完程式碼要記得的事：** 只要動到畫面或計算，
就得重新 `export` 並發佈，否則手機上看到的還是舊的。
不想在本機做的話，推上 `main` 之後到 Actions 手動觸發 `每日排行快照`
（`workflow_dispatch`，可以指定只回補幾個交易日）也會順便重新發佈。
若今日行情尚未公布、只需要把已推送的畫面修正發布出去，手動觸發時可勾選
`publish-only`；它只使用 `data` 分支現有快取重新 export／發布，不回補今日行情、不同步 Supabase，
也不改動 `data` 分支。這個選項不能取代收盤後的完整快照流程。

## 常見雷

| 症狀 | 原因 |
|---|---|
| `找不到環境變數 SUPABASE_DB_URL` | 沒設、設在別的 shell profile，或是在非互動 shell 裡跑（要用 `zsh -ic`） |
| `pg_dump: server version mismatch` | 本機 `pg_dump` 比 Supabase（17.6）舊 |
| 排行頁一片空白、說資料不足 | 沒有行情快取，先取 `data` 分支或 `backfill` |
| 手機上看到舊資料 | 沒有重新發佈；或按一下標題旁的「檢查更新」 |
| 改了 `CollectionSchedule` 的時間但排程沒變 | cron 寫在兩支 YAML 裡，讀不到 C#，要手動改（TODO 8 未決） |
| `dotnet test` 找不到 SDK | 裝的是 .NET 8/9，這個專案要 10 |
| push `.github/workflows/*.yml` 被拒 | token 少了 `workflow` scope |
