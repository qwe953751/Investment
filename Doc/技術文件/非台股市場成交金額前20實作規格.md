# 非台股市場（美／日／韓）成交金額前 20：實作規格

> 撰寫日期：2026-09-18
> 文件性質：**交接用實作規格**，給接手的模型或開發者直接照做
> 需求來源：筆記 #70
> 前置評估：[美股成交金額前20免費資料源與公開展示評估](美股成交金額前20免費資料源與公開展示評估.md)
> **狀態（2026-09-19）：工項 A～E 已全部實作並本機驗證完成**（`YahooScreenerMarketTurnoverClient` 取代
> Massive／KIS，本機對 us/jp/kr 三市場實測涵蓋證明皆成立，`dotnet test` 全綠，`MarketTurnoverCdn:Public`
> 已開啟）；下面內文仍保留原始事故描述與規劃過程供追溯，最新結論以 [TODO.md](../TODO.md#todo-16)
> 2026-09-19 節與 [完成進度.md](../完成進度.md) 為準，Storage 實際發布與線上驗收待 GitHub Actions 跑過後補記。

## 0. 本文件的前提，請先讀

使用者已明確裁示：**先不管條款，先做出可以運作的東西，備案另尋。**

因此本規格採用 Yahoo Finance 的未公開 screener endpoint。這個選擇在授權上是有問題的
（Yahoo Finance 說明頁明文 "You must not redistribute information displayed on or provided by
Yahoo Finance"），前置評估文件 §6.4 也曾否決這類來源。這裡是**在知情的前提下刻意接受風險**，
不是遺漏。接手者不需要再重新評估一次條款，也不要因為條款疑慮就自行改用別的來源或停工；
但**也不要在程式或文件裡宣稱本方案已取得授權**。

備案路線（TradingView Widget、付費授權）維持在前置評估文件裡，本文件不處理。

另外一個必須知道的事實：專案**現在就已經在用 Yahoo Finance** 供應美股四大指數、11 大類股 ETF、
加密貨幣、日經與 KOSPI（`YahooFinanceDailyQuoteClient`／`YahooFinanceIntradayQuoteClient`），
且結果已寫入公開的 `data/market-overview.json`。本規格沒有引入新的風險類別，只是沿用同一條。

---

## 1. 現況：有一個正在進行中的事故，必須先修

`us-daily-snapshot.yml` 與 `asia-market-overview-daily.yml` 連續數日紅燈，原因相同：

```
System.InvalidOperationException: 未設定 Massive 金鑰。需要環境變數 MASSIVE_API_KEY；不使用假值。
System.InvalidOperationException: 未設定 KIS 金鑰。需要環境變數 KIS_APP_KEY 與 KIS_APP_SECRET；不使用假值。
→ Process completed with exit code 134
```

`gh secret list` 確認 repo 只有 `ALPHA_VANTAGE_API_KEY`、`FRANK_INVEST_PAT`、`SUPABASE_DB_URL`、
`SUPABASE_STORAGE_SECRET_KEY`。`MASSIVE_API_KEY`／`KIS_APP_KEY`／`KIS_APP_SECRET` **從未設定過**，
所以 `market-turnover` 指令從上線第一天就沒有成功過，`data` 分支也沒有 `imports-turnover/` 目錄。

真正的傷害來自步驟順序——`market-turnover` 排在提交之前：

| workflow | turnover 步驟 | 提交步驟 | 被連帶擋掉的 |
|---|---|---|---|
| `us-daily-snapshot.yml` | L113–116 | L118–141 | commit、`sync`、`verify`、警報更新 |
| `asia-market-overview-daily.yml` | L59–63 | L65–84 | commit |

`data` 分支落後狀況（2026-09-18 實查）：

| 目錄 | 最新一天 | 落後 |
|---|---|---|
| `imports/`（台股） | 2026-09-18 | 正常，不受影響 |
| `imports-overview/`（美股＋日韓＋加密） | 2026-09-14 | 4 天 |
| `imports-us/`（美股個股） | 2026-09-11 | 7 天 |
| `imports-turnover/` | 不存在 | 從未產生 |

原設計註解寫「品質門檻未過就不留下半套快取」，立意正確，但實際效果是
**一個還沒上線的功能，擋掉了三個已經上線的功能**。這是本規格要修掉的第一件事。

---

## 2. 範圍

| 市場 | 指數 | 成交金額前 20 | 更新頻率 |
|---|---|---|---|
| 美股 | 已完成，不動 | 本規格新做 | 盤後 |
| 日股 | 已完成，不動 | 本規格新做 | 盤中＋盤後 |
| 韓股 | 已完成，不動 | 本規格新做 | 盤中＋盤後 |
| 加密貨幣 | 已完成，不動 | 不做 | — |

台股完全不在本規格範圍內，一行都不要動。

---

## 3. 工項 A（P0）：讓未上線功能停止擋住已上線功能

**目標**：無論排行成功與否，既有的行情回補、提交、同步、對帳、警報都要照跑。

`MarketTurnoverCollector.CollectAsync` 本身已經是 per-market try/catch，會把失敗收進
`SkippedMarkets`／`Warnings` 而不丟出。所以 exit 134 是**指令層**在有 warning 時主動失敗。
請找到 `market-turnover` CLI 指令的結束碼邏輯，改成：

- 至少一個市場成功 → exit 0，warning 印出來。
- 全部市場失敗 → **仍然 exit 0**，但把 warning 印出來，並在 workflow 用獨立的非阻斷步驟回報。
- 真正該讓 workflow 紅燈的是「已經公開發布的排行突然變成空的」，不是「還沒接好的來源沒資料」。

同時在兩個 workflow 的 turnover 步驟加上 `continue-on-error: true` 作為第二層保險。

**驗收**：故意不給任何來源設定，跑 `us-daily-snapshot.yml`（`workflow_dispatch`），
必須綠燈，且 `data` 分支有當天的 `imports-us/`、`imports-overview/`。
完成後手動補跑，把落後的 7 天美股與 4 天總覽補回來。

這一項與後面的工項彼此獨立，**先做、先發布、先確認網站恢復**，再做工項 B。

---

## 4. 工項 B：新來源 `YahooScreenerMarketTurnoverClient`

新增檔案：`src/Invest.Web/Infrastructure/MarketData/Turnover/YahooScreenerMarketTurnoverClient.cs`

### 4.1 取得 cookie 與 crumb（必要，不可略過）

Yahoo screener 需要一組 cookie ＋ crumb，兩者缺一就回 `Unauthorized`。

```
步驟 1：GET https://fc.yahoo.com
        → 預期 HTTP 404，這是正常的；目的只是讓回應的 Set-Cookie 寫入 CookieContainer
        → 必須取得 .yahoo.com 網域的 A3 cookie
步驟 2：GET https://query1.finance.yahoo.com/v1/test/getcrumb
        → 帶上步驟 1 的 cookie
        → 回傳純文字 crumb，例如 tJ.8wope7I4
```

實作要點：
- `HttpClientHandler { CookieContainer = new(), UseCookies = true }`，或用 `IHttpClientFactory`
  註冊一個帶 `CookieContainer` 的 named client。
- **步驟 1 的 404 不可視為失敗**，只有「拿不到 cookie」才算失敗。
- 每次 collect 開始時取一次 crumb，整輪重用；收到 `Unauthorized` 時重取一次，再失敗才放棄。
- `User-Agent` 必須是一般瀏覽器字串，不能是預設的 .NET UA。

已實測：無 cookie 時 `getcrumb` 回
`{"finance":{"result":null,"error":{"code":"Unauthorized",...}}}`；有 cookie 時正常回 crumb。

### 4.2 Screener 請求

```
POST https://query1.finance.yahoo.com/v1/finance/screener
     ?crumb={crumb}&lang=en-US&region=US&formatted=false
Content-Type: application/json
```

Body：

```json
{
  "size": 250,
  "offset": 0,
  "sortField": "dayvolume",
  "sortType": "DESC",
  "quoteType": "EQUITY",
  "query": {
    "operator": "AND",
    "operands": [{ "operator": "eq", "operands": ["region", "us"] }]
  },
  "userId": "",
  "userIdType": "guid"
}
```

- `region` 取 `us` / `jp` / `kr`，對應本專案的市場代碼，**不需要轉換**。
- `size` 上限 250。`offset` 實測可到 9750；**offset ≥ 10000 會回傳與 9750 相同的內容**
  （靜默重複，不報錯），所以分頁上限必須硬編在 10000，並且要偵測「這一頁的第一個代號
  與上一頁相同」時中止，避免無限迴圈。

實測回傳的全市場總數：

| region | `total` |
|---|---|
| us | 20,175 |
| jp | 3,838 |
| kr | 2,675 |

### 4.3 欄位對映（`MarketTurnoverRow`）

| 契約欄位 | 來源欄位 | 備註 |
|---|---|---|
| `Symbol` | `symbol` | 日股帶 `.T`、韓股帶 `.KS`／`.KQ`，**原樣保留** |
| `Name` | `shortName` ?? `longName` ?? `symbol` | |
| `LastPrice` | `regularMarketPrice` | |
| `Turnover` | `regularMarketPrice × regularMarketVolume` | **估算值，見 §4.6** |
| `Currency` | `currency` | USD／JPY／KRW |
| `ChangePercent` | `regularMarketChangePercent` | 已是百分比數值，不要再乘 100 |
| `Market` | 呼叫端傳入 | `us`／`jp`／`kr` |
| `Rank` | 排序後填 1..20 | |
| `Source` | `"yahoo-screener"` | 見 §4.6，不可寫成交易所或 VWAP |

`regularMarketPrice` 或 `regularMarketVolume` 任一為 null 的列直接丟棄，不要用 0 代入。

其他可用欄位供參考：`regularMarketPreviousClose`、`fullExchangeName`、`marketState`、
`regularMarketTime`（epoch 秒）、`exchangeDataDelayedBy`（日韓實測為 `20`，即延遲 20 分鐘）。

### 4.4 演算法：雙軸候選池 ＋ 可證明的截斷

不能只抓「成交量前 N 檔再乘價格」——那會漏掉高價低量股，前置評估文件 §6.1 已經點名這是錯的。
但也不需要把 20,175 檔全抓下來。正確做法是**抓兩個軸的極端值，然後證明沒抓到的不可能進前 20**：

```
1. 沿 sortField=dayvolume     DESC 分頁抓 N 頁 → 集合 A
2. 沿 sortField=intradayprice DESC 分頁抓 M 頁 → 集合 B
3. 候選池 = A ∪ B（用 symbol 去重）
4. 對候選池每一檔算 turnover = price × volume，由大到小取前 20
5. 安全性證明：
       V_min = A 之中最小的 volume
       P_min = B 之中最小的 price
       任何不在候選池的標的，volume < V_min 且 price < P_min
       → 它的 turnover < V_min × P_min =: bound
   若 bound < 第 20 名的 turnover，則這份前 20 與掃描全市場的結果完全相同。
6. 若 bound ≥ 第 20 名 turnover，加深 N 與 M 重跑；連續加深仍不成立就視為失敗，
   不得發布（見 §7）。
```

這一步是本規格的核心，**必須實作，而且 `bound < cutoff` 要當成硬性品質門檻**，
不能只當 log。使用者的既有規則是「排行榜要全市場算，不能只取前 N 檔」；
這個證明就是滿足該規則的方式——結果與全市場掃描等價，且可驗證。

建議起始頁數（已實測足夠，留有數百倍餘裕）：

| market | N（量軸頁數） | M（價軸頁數） | 請求數 |
|---|---|---|---|
| us | 12 | 2 | 14 |
| jp | 8 | 2 | 10 |
| kr | 8 | 2 | 10 |

每次請求之間 sleep 250ms。整輪三個市場約 34 次請求。

### 4.5 實測結果（2026-09-18，此演算法的實際輸出）

| market | 候選池 | 未掃到者上限 `bound` | 第 20 名 turnover | 安全 |
|---|---|---|---|---|
| us | 3,304（量 2,988 ∪ 價 486） | 0 USD | 2,091,969,357 USD | ✅ |
| jp | 2,180（量 2,000 ∪ 價 500） | 147,171,200 JPY | 74,640,646,500 JPY | ✅ 約 500 倍餘裕 |
| kr | 2,094（量 1,996 ∪ 價 500） | 0 KRW | 208,618,331,000 KRW | ✅ |

美股與韓股的 `bound` 為 0，是因為量軸分頁已經翻到成交量為 0 的標的，代表所有「今天有成交」
的標的都已納入候選池。

前 5 名輸出範例，可作為接手實作時的對照基準（數字會隨交易日變動，看的是量級與名單合理性）：

```
US : MU 13.3B / SNDK 12.6B / NVDA 11.0B / TSLA 8.8B / GOOGL 7.1B  (USD)
JP : 285A.T 1,881B / 9984.T 570B / 6857.T 347B / 6976.T 243B / 8035.T 213B  (JPY)
KR : 000660.KS 8,847B / 005930.KS 4,565B / 009150.KS 1,218B / 010170.KQ 1,017B  (KRW)
```

驗證腳本保留在 `/tmp/turnover_probe.py`（拋棄式，不進 repo；若需要可依 §4.2–4.4 重寫）。

### 4.6 精度：必須誠實標示

`turnover = 最新價 × 當日成交量` 是**估算值**，不等於交易所公布的成交金額
（交易所是逐筆成交金額加總）。原本的 Massive 來源用 `volume × VWAP`，比本方案準。

因此：

- `Source` 欄位固定寫 `"yahoo-screener"`，不要寫成交易所名稱。
- 前端顯示成交金額的區塊要標示「估算值，依最新價×成交量計算」與資料延遲。
- **不要**把這個數字拿去跟台股的真實成交金額做同一套比較或加總。

可選的精度改善（不是必要工項）：對前 60 名候選另外呼叫 chart API 取當日
`(high + low + close) / 3` 當 VWAP 近似值重算。成本約 60 次請求。**第一版不要做**，
先讓功能上線。

---

## 5. 工項 C：接線

### 5.1 `MarketTurnoverCollector`

現況（`MarketTurnoverCollector.cs:26-28`）：

```csharp
var rows = market == "us"
    ? await massiveClient.GetAsync(tradingDate, cancellationToken)
    : await kisClient.GetAsync(market, tradingDate, cancellationToken);
```

改為統一走新來源：

```csharp
var rows = await yahooScreenerClient.GetAsync(market, tradingDate, cancellationToken);
```

`YahooScreenerMarketTurnoverClient.GetAsync` 的簽章比照 `KisMarketTurnoverClient`：
`Task<IReadOnlyList<MarketTurnoverRow>> GetAsync(string market, DateOnly tradingDate, CancellationToken)`。

### 5.2 舊來源怎麼處理

`MassiveMarketTurnoverClient` 與 `KisMarketTurnoverClient` 從未成功執行過，且沒有金鑰。
**直接刪除這兩個檔案**，連同 `appsettings.json` 的 `KisMarketData`（L51）、
`MassiveMarketData`（L57）兩個區段、DI 註冊、以及兩個 workflow 裡的
`MASSIVE_API_KEY`／`KIS_APP_KEY`／`KIS_APP_SECRET` 環境變數宣告一起移除。

不要保留成「暫時停用」或加 feature flag 在兩個來源之間切換——沒有金鑰就切不回去，
留著只會變成看起來還有備援的死碼。備案路線寫在文件裡就夠了。

### 5.3 新增設定

`appsettings.json` 新增：

```json
"YahooScreenerMarketData": {
  "BaseUrl": "https://query1.finance.yahoo.com",
  "PageSize": 250,
  "MaxOffset": 10000,
  "RequestDelayMilliseconds": 250,
  "VolumePages": { "us": 12, "jp": 8, "kr": 8 },
  "PricePages": { "us": 2, "jp": 2, "kr": 2 }
}
```

不需要任何 secret，這是本方案最大的操作優勢。

---

## 6. 工項 D：排程與盤中

### 6.1 美股（盤後）

`us-daily-snapshot.yml` 既有的 `market-turnover --markets us`（L113–116）保留，
移除金鑰環境變數。依筆記 #70，美股**只要盤後**，不做盤中。

### 6.2 日韓（盤中＋盤後）

- 盤後：`asia-market-overview-daily.yml` 既有的 `market-turnover --markets jp,kr`（L59–63）保留。
- 盤中：掛進既有的 `asia-market-overview-intraday.yml`，沿用它現有的
  「只在東京／首爾交易時段執行、日股午休不抓」的時段判斷，不要另外寫一套。
  盤中輪次 `isFinal=false`，`MarketTurnoverCollector` 已處理：**盤中不寫 `data` 分支**，
  只送版本化 Supabase Storage 快照與 `latest.json`。這條邏輯不要改，
  它是為了避免重演台股盤中直連的流量問題。

盤中資料延遲 20 分鐘（`exchangeDataDelayedBy: 20`），前端要標示。

---

## 7. 品質門檻與失敗行為

`MarketTurnoverQualityGate.EnsureComplete` 既有的四項檢查全部保留：
至少 20 列、代號不重複、名次 1..N 連續、金額與代號有效。

**新增第五項**：§4.4 的截斷證明 `bound < 第20名 turnover`。不成立就丟
`MarketTurnoverDataIncompleteException`，該市場算失敗。

失敗時的行為（重要，跟工項 A 是同一個原則）：

- 單一市場失敗 → 其餘市場照常發布；失敗市場**保留既有快照**，不發布部分資料。
- 前端在缺資料時維持現有文案：「目前快照尚未提供成交金額前 20；未以指數或產業代表標的代替。」
  （`site.js` 約 L26619）**不要**加入示意資料或回退到舊排行冒充當日。
- 全部市場失敗 → 指令仍 exit 0（見工項 A），但要留下清楚的 warning。

---

## 8. 工項 E：公開發布

完成工項 B 並在本機確認三個市場都能穩定產出後：

1. `MarketTurnoverCdn:Public`（`appsettings.json` L63 區段）由 `false` 改為 `true`。
2. 確認 `SUPABASE_STORAGE_SECRET_KEY` 已存在（已確認存在）。
3. 先跑一輪盤中，確認 Storage bucket `market-turnover-snapshots` 有版本化快照與 `latest.json`。
4. 再跑 export 與網站發布，確認 `manifest.json` 指向排行 CDN。

順序不可顛倒——`Public=false` 時 export 不會把排行 CDN 寫進 manifest，
盤中輪次也會因為 `publishResult.Published == false` 而失敗。

---

## 9. 測試

新增 `tests/Invest.Web.Tests/YahooScreenerMarketTurnoverClientTests.cs`，以錄下來的
JSON 回應做離線測試，至少涵蓋：

1. 欄位對映正確，含 `.T`／`.KS`／`.KQ` 後綴保留。
2. `regularMarketPrice` 或 `regularMarketVolume` 為 null 的列被丟棄，不以 0 代入。
3. 分頁在「本頁第一個代號與上一頁相同」時中止（模擬 offset ≥ 10000 的靜默重複）。
4. 截斷證明成立與不成立兩種情境：不成立時必須丟
   `MarketTurnoverDataIncompleteException`。
5. 高價低量股確實能進入前 20（構造一檔 volume 排在 3,000 名外、但 price 極高的標的，
   驗證它有被價軸候選池撈到）——這是本演算法存在的理由，必須有測試釘住。

既有的 `MarketTurnoverQualityGate` 測試不要改。

---

## 10. 驗收

工項 A（先做）：

- [ ] 沒有任何排行來源設定時，`us-daily-snapshot.yml` 與 `asia-market-overview-daily.yml` 綠燈。
- [ ] `data` 分支的 `imports-us/`、`imports-overview/` 補回到當日。
- [ ] Supabase `sync`、`verify`、警報步驟恢復執行。

工項 B–E：

- [ ] 三個市場各自產出 20 列，名次連續、代號不重複、金額為正。
- [ ] 截斷證明在三個市場都成立，且 log 印出 `bound` 與 cutoff 兩個數字供人工核對。
- [ ] 前 5 名與 §4.5 的量級相符（同一交易日可直接比對）。
- [ ] 日韓盤中輪次只寫 Storage，`data` 分支當天沒有被反覆提交。
- [ ] 美股沒有盤中輪次。
- [ ] 任一市場來源失敗時，其他市場照常發布，失敗市場顯示既有空狀態文案而非示意資料。
- [ ] 公開網站（無痕視窗）能看到三個市場的排行，且標示了估算值與延遲。
- [ ] Supabase PostgreSQL 沒有因為排行新增任何讀寫。

---

## 11. 已知限制

1. **授權**：見 §0。這是刻意接受的風險，備案見前置評估文件。
2. **精度**：成交金額是 `最新價 × 成交量` 估算值，非交易所公布值（§4.6）。
3. **沒有 SLA**：Yahoo 未公開端點隨時可能變更、擋 IP 或回 429。既有的
   `MarketOverview` 路徑就曾遇過 429。失敗時的行為已在 §7 定義，不要加重試風暴。
4. **cookie/crumb 機制可能改變**：這是最脆弱的一環。若哪天 `getcrumb` 開始要求
   JS 驗證（Stooq 今年就是這樣掛掉的），本方案會整個失效，屆時只能走備案。
5. **歷史回補做不到**：screener 只回傳「當下」的報價，沒有歷史交易日參數。
   所以排行只能從上線日開始往前累積，**無法回補過去兩年**。
   這點與既有 `market-overview` 的日期選擇器行為不同，前端的排行日期選擇器
   只能提供已累積的日子。
