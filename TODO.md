# 待辦事項（15 件）

這份檔案是討論的存放處，不是進度表。每次要談某件事之前先讀這裡，
就不用把前幾次的結論重講一遍。

**已經做完的事不寫在這裡**，看 [Doc/完成進度.md](Doc/完成進度.md)。
一件事上線並驗收完成之後就從這裡移除，只留還沒定案或還在等的。

## 快速跳轉

點擊下表的待辦事項，即可直接跳到下方完整討論內容。

| # | 待辦事項 | 狀態 |
|---|---|---|
| 1 | [盤中與盤後的比較策略](#todo-1) | 🔵 進行中 |
| 2 | [寫入帳號拆分：invest_writer_pc / invest_writer_ci](#todo-2) | ⚪ 未開始 |
| 3 | [權限模型：密碼登入三層權限（前端已上線，RLS 收權限待驗收）](#todo-3) | 🟡 等驗收 |
| 4 | [Supabase Management API token 到期](#todo-4) | 🟡 等時間 |
| 5 | [盤後「抓到有為止」碰上國定假日會誤紅燈](#todo-5) | 🟡 等假日驗收 |
| 6 | [記住上次選的選項，重整後不要跳回預設](#todo-6) | 🟡 等實地驗收 |
| 7 | [盤中成交額準確度：對照已經在背景收，等收滿六天](#todo-7) | 🟡 等資料 |
| 8 | [已合併的遠端分支要不要刪](#todo-8) | 🟡 等你決定 |
| 9 | [盤中「成交比變化」疑似每列都是 +0.01%](#todo-9) | 🟡 等盤中確認 |
| 10 | [盤中輪距要不要縮到 1 分鐘](#todo-10) | 🟡 不建議，維持 2 分鐘 |
| 11 | [收攏成單一網址：admin888／viewer 已收掉，改單一根目錄＋登入分層（程式已 commit，已發布）](#todo-11) | 🟡 已發布，舊 repo／iframe 待決定 |
| 12 | [新聞熱度目前在量「節點多大」而不是「題材多熱」，要基準線才修得掉](#todo-12) | 🟡 等資料 |
| 13 | [GitHub 排程事件晚到 6～13 小時，自動收集與每日快照都可能整天沒跑](#todo-13) | 🟡 8/31 驗收又抓到兩個成因（run 層級鎖、鬧鐘被純發布騙），都已修，等 9/1 驗收 |
| 14 | [Supabase 流量超額，9/27 起適用 Fair Use Policy](#todo-14) | 🟡 已改走 CDN，等 8/31 量實際流量 |
| 15 | [D+ AI OCR：Golden Set、Validator 與正式 Worker](#todo-15) | 🟡 管線已整合，等正式站／Golden Set／Windows 驗收 |

狀態只有三種：🔵 進行中、🟡 等資料或等時間、⚪ 未開始。

每一項的格式固定：**已討論的結論**寫清楚為什麼這樣決定，
**尚未討論的**寫清楚卡在哪裡、需要什麼才談得下去。
結論改變就直接改這裡，不要在下面追加「更正」。

增刪項目時，標題的件數、上面的表、下面的編號要一起改。

---

<a id="todo-1"></a>
## 🔵 1. 盤中與盤後的比較策略

[↑ 回到 TODO 列表](#快速跳轉)

**狀態：主體已上線，等真實盤中資料驗收**

### 已討論

原始需求第 5 點。談完的結論是**用市場成交比來比，不要用成交額來比**。

問題出在「今天還沒過完」：半天的量拿去比人家一整天的量，一定小。
如果先把今天的量推估成全日再比，那準確度就整個綁在預估值上——
早盤預估值的分母原本是線性時間比例，現已改用校準過的日內量能曲線
f(t)（2026-09-05，筆記 #42，見 [Doc/完成進度.md](Doc/完成進度.md)），
但盤中與盤後比較這件事本身仍然選擇繞開預估值，見下面的作法。

改看比例就繞開了這件事：

```
盤中成交比 = 個股今日累計 ÷ 全市場今日累計
成交比變化 = 盤中成交比 − 過去 N 日的市場成交比（百分點）
```

分子與分母取自**同一輪**，時段進度會自己約掉，所以 09:05 就能看。
展開來說：

```
盤中成交比 = 真實日成交比 × (個股量能形狀 ÷ 大盤量能形狀)
```

誤差從「整條 U 型曲線」縮到「這一檔的形狀跟大盤的形狀差多少」，是二階的；
而且形狀差很多本身就是有意思的訊號（例如開盤爆量的個股）。

**已實作（純前端，沒有後端改動）**：靜態站匯出的 `data/{期間}-{日期}.json`
本來就含全部個股與 `share`，盤中頁直接抓同一份檔案，用股號 join。

- 盤中新增兩欄可排序：**市場成交比**、**成交比變化**
- 盤中的**觀察期間**按鈕重新解讀為「跟過去 N 個交易日對照」，交易日選擇器仍然隱藏（盤中永遠是今天）
- 盤中與盤後的**資金加速**現在都改用收縮量比排序（筆記 #10，2026-09-05，見
  `AccelerationRules.ShrunkRatio`），不再各自維護「成交比變化」與「量比」兩套指標

原本還做了一欄**倍數**（今日預估成交額 ÷ 過去 N 日均量）當參考值，後來刪掉了：
那是驗證方法 B 的時候用的，成交比變化上線之後它就沒有讀者了。

### 尚未討論

- 上方訊息欄（重大消息、新聞）：使用者認為架構還不 OK，**先不做**。
- 要不要在盤中直接標出「今天會擠進前 N 名」的個股。這需要預估值可信，
  預估值本身已經改用校準曲線（筆記 #42）可信度提升，但這個標記功能還沒做。
- ~~盤中要不要也給成交門檻~~：已解決（筆記 #10，2026-09-05）。盤中資金加速
  現在跟盤後共用同一套 `currentLiquidityFloor`（當期流動性門檻），不再只靠
  `state.threshold` 擋。
- 交易限制標記（處置、全額交割）目前只有「現在」的狀態：四個來源都沒有歷史查詢
  （上市的處置 API 吃日期區間，其餘完全不吃），所以往回選基準日時標記講的還是現在。
  要做到逐日正確得自己每天存一份，但那要先確定真的會回頭看歷史的限制狀態，現在還不確定。

---

<a id="todo-2"></a>
## ⚪ 2. 寫入帳號拆分：invest_writer_pc / invest_writer_ci

[↑ 回到 TODO 列表](#快速跳轉)

### 已討論

現在本機與 GitHub Actions 共用同一組 `invest_writer` 連線字串。
拆開的好處是出事時看得出是誰寫的、可以單獨撤銷其中一組。

明確**不做**的事：`grant postgres to invest_writer`。
CI 的密碼放在 GitHub Secrets，不能讓它擁有 DDL 權限。
資料表變更一律走 Supabase Management API（見 README 的「資料表定義」）。

### 尚未討論

拆分之後 RLS policy 要怎麼寫、值不值得為此多維護一組密碼。

---

<a id="todo-3"></a>
## 🟡 3. 權限模型：密碼登入三層權限

[↑ 回到 TODO 列表](#快速跳轉)

### 已討論

**2026-09-01 設計已改變並上線（筆記 #37）**：先前定案的「Google 登入 + `editors`
白名單」方向**沒有採用**，改成更簡單的密碼登入＋固定三層：訪客（不登入）／
監控者／最高權限。原因是 Google OAuth 對靜態站（無後端）串接複雜、且使用者
只有自己和另一個信任對象要登入，不需要白名單擴充彈性。

實作方式：

- 頁首新增獨立登入列（`#access-bar`），隨時顯示目前權限，密碼輸入比對兩組
  Supabase Auth 固定帳號：`admin@investment.local` → 最高權限、
  `monitor@investment.local` → 監控者。帳號密碼由 `db/029_permission_accounts.sql`
  以 `auth.users`／`auth.identities` 直接建立（不透過註冊流程寄信）。
- 登入狀態是疊加在既有網址下限（`URL_ACCESS`，admin888／viewer，見第 11 項）
  之上的，`applyEffectiveAccess()` 取兩者較高者；登入只會往上疊加，不會蓋掉
  網址本身的下限。
- 同裝置靠 Supabase 的 refresh token 存在 `localStorage`（key `invest.auth`）
  自動恢復登入；另外支援 `?key=密碼` 網址參數做長者友善的免打字自動登入，
  用過即從網址列移除。
- 前端已完整實作並端到端驗證（真密碼登入、頁籤依權限篩選、自動恢復都測過），
  commit `6ddd72d4`（`main` 已推送）。`db/029_permission_accounts.sql` 已用
  Management API 套用到正式 Supabase，兩組帳號皆可正常登入。

### 尚未討論／待驗收

- **RLS 尚未收權限**：`notes`／`topic_edits`／資產相關表（含持倉、出入金與每日快照）目前仍是 anon
  可讀可寫，跟登入前完全一樣——這次只做了前端登入與頁籤切換，故意不動 RLS。
  等使用者實際用過登入功能、確認沒問題後，才會有下一支 migration 把這幾張表的
  政策換成「只認這裡建的帳號」（見 `db/029_permission_accounts.sql` 檔頭與
  `015_notes.sql` 的既定順序：先讓登入頂上，才能收權限）。
- `db/029_permission_accounts.sql` 內容含明碼密碼（`crypt(...)` 的字面參數），
  repo 是公開的，已加進 `.gitignore` 排除、永遠不進版控；套用方式記錄在
  memory `reference_supabase_ddl`，需要在別的環境重建帳號時照那份做。
- 兩組密碼是使用者自訂，不記錄在任何會進版控的檔案。

---

<a id="todo-4"></a>
## 🟡 4. Supabase Management API token 到期

[↑ 回到 TODO 列表](#快速跳轉)

**狀態：等時間（約 2026-09-15）**

### 已討論

`SUPABASE_ACCESS_TOKEN` 放在 `~/.zshrc`，建立於 2026-08-16，效期 30 天。
到期後只影響**資料表變更**（貼 `db/*.sql`），不影響每日回補、同步、備份——
那些走的是 `SUPABASE_DB_URL`（`invest_writer`），沒有期限。

到期後在 Supabase 後台重新產生一把，換掉 `~/.zshrc` 裡那一行即可。

---

<a id="todo-5"></a>
## 🔵 5. 盤後「抓到有為止」碰上國定假日會誤紅燈

[↑ 回到 TODO 列表](#快速跳轉)

**狀態：已用官方休市日曆解掉，等下一個平日假日實地驗收（2026-09-25 中秋節）**

### 已討論

排程改成 16:07 先卡住 runner、睡到 18:00 才開抓，之後每 10 分鐘重跑一次
到 21:00 為止，直到 `data/imports/{今天}.json` 落地。
這是為了兌現「說好 18:00 就是 18:00，而且抓到有為止」。

**代價**：程式分不出「今天是國定假日」與「官方還沒公布／被擋」。
`MarketDataDownloader` 對於**今天**的日期，兩個市場都空的時候一律回 `null`
（訊息是「官方尚未公布收盤行情，這次先不記錄」），兩種情況長得一模一樣。
非交易日一旦寫進快取就不會再重試，所以不能亂猜。

結果是**每年約 10 個平日的國定假日**（春節、清明、端午、中秋、國慶⋯⋯）
會一路重試到 21:00 然後紅燈。紅燈本身不會弄壞資料，只是會發通知。

**做法（已實作）**：`TwseHolidayCalendar` 抓證交所的官方休市日曆
（`openapi.twse.com.tw/v1/holidaySchedule/holidaySchedule`，一次給整年），
只在上面那個判斷點用一次：日曆明講今天休市，才寫成非交易日；
**日曆沒說、或根本讀不到，一律維持原本的「先不記錄、繼續重試」**。
這樣日曆壞掉的最壞下場只是紅燈，不會把交易日寫死成休市——後者是補不回來的。

寫成非交易日之後，`data/imports/{今天}.json` 就會落地，重試迴圈自己綠燈收工。

**這份清單不能照單全收**：27 筆裡有 3 筆是「農曆春節前最後交易日」這種
**照常開市**的特殊日子，所以名稱帶「交易日」的要濾掉。
濾完的 24 天拿 320 天的本機快取對過，沒有任何一天是真實交易日。

### 盤中的臨時休市（颱風假）走另一條路（2026-08-23 已實作）

台北市宣布停班就不開盤，而且是當天清晨才宣布，排程不可能事先知道，
證交所的年度休市日曆也不會有。但**不必去讀停班公告**：
真的休市時 MIS 照樣回應、只是日期停在上一個交易日，收集器本來就認得出來。

所以判斷條件是「長相」而不是「公告」：到 `CollectionSchedule.IntradayGiveUp`（10:00）
為止一輪都沒寫進當日資料、而且**沒有任何一輪是失敗或被丟掉**，就判定沒開盤、提早收工。
有失敗就分不出休市與故障，寧可繼續跑到 13:35。

- 開盤是 09:00，留到 10:00 才判，是因為公告可能出錯、開盤也可能延後；
  這中間有 30 輪，證據夠了（`CollectionScheduleTests` 有一條在守這個下限）。
- 使用者要的就是這個：「抓到颱風假，也繼續抓資料到 10:00，如果都沒有，再停掉」。
- 提早收工走的是正常結束，不是例外，所以網站上的鈴鐺不會因此亮紅點。

### 尚未討論

- 跨年時新一年的清單什麼時候公布。沒公布的話 1 月初的假日會退回紅燈。

---

<a id="todo-6"></a>
## 🟡 6. 記住上次選的選項，重整後不要跳回預設

[↑ 回到 TODO 列表](#快速跳轉)

**狀態：已上線，等明天 07:00–18:00 實際用過一輪**

### 已討論

需求：**第一次開網站的預設值維持現在這樣**，但之後按重整時要沿用上一次選的，
不要每次都跑回預設。

現在所有選項都放在 `site.js` 的 `state`，寫死在原始碼裡、每次載入都重來：

| 欄位 | 現在的預設 | 是什麼 |
|---|---|---|
| `view` | `'daily'` | 盤後／盤中 |
| `period` | 盤後 1 日／盤中 5 日 | 觀察期間幾個交易日 |
| `date` | manifest 最新的一天 | 基準交易日 |
| `mode` | `'heat'` | 排行模式 |
| `market` | `'all'` | 上市／上櫃 |
| `threshold` | 1 億 | 平均日成交值門檻 |
| `sortKey` / `sortDescending` | `'rank'` / `false` | 排序 |

做法上不用另外發明：鎖定股號已經是這個模式（`localStorage`、
key 是 `invest.lockedTickers`、讀寫都包 try/catch 讓無痕模式不會炸）。

**記憶的有效期跟著取資料的時間走（已定案）**：

```
07:00  盤中收集開跑  → 從這裡開始記上次的設定
18:00  盤後回補開跑  → 存的東西作廢，回到預設
```

也就是 07:00–18:00 之間選的設定會被記住、重整不會跑掉；
跨過 18:00 之後再開就是全新的預設值，因為那時候換的是新一天的盤後資料。
`view` 與 `date` 不另外訂規則，一起吃這個有效期——18:00 之後開一定是盤後頁、
一定是最新一天，早上七點以後才開始沿用。

**這兩個時間不可以在前端寫死。** 使用者的原話是「盤中與盤後獲取資料時機同步，
避免下次換時間就不一樣了」：改了排程卻忘了改前端，記憶就會在錯的時間點作廢。
要有單一來源，由後端寫進 `manifest.json`，`site.js` 讀它。

**存著的舊值一律驗證，驗不過就退回預設（已定案）**。
例如 `PERIODS` 或門檻級距改版之後，存的值可能已經不存在。

**不寫進網址列（已定案）**。使用者明確說不要改網址，分享連結維持乾淨。

**實作（已上線）**：

- 時間的單一來源是 `CollectionSchedule`（C#），由 export 寫進 `manifest.json` 的
  `schedule`，`site.js` 讀它。前端刻意**不留預設時間**：抄一份就會漏改。
  manifest 給不出 `schedule` 時就整個關掉記憶功能，一律用預設選項。
- 記憶期用「台北日期」當標記存進 `localStorage`（`invest.settings`）。
  存的時候不在期內就不寫；讀的時候標記對不上就整份忽略。
  這樣「跨過 18:00 就作廢」不必真的去比時間差。
- 每一項分開驗（期間、基準日、模式、市場、門檻、排序欄位），
  驗不過的那一項留在預設，不會因為一項壞了整組丟掉。
  門檻只驗「是不是合理的數字」，因為它可以自己輸入任意金額。
- `view` 額外擋一層：沒有資料庫連線時盤中頁是停用的，存著的值不能繞過。
- 盤中那句說明（收集器幾點開始、幾分鐘一輪）以前是寫死的 08:40，
  現在也讀 `schedule`，不會再跟排程講不一樣的話。

瀏覽器實測過四種情況：期內選了會記、重整後回得來（含排序欄位）、
跨日標記過期回預設、期外（22:39）選了完全不寫。塞壞值進去只有合法的兩項生效。

**觀察期間的預設值兩邊不一樣（已定案）**：盤後 1 日、盤中 5 日。
盤後回答的是「昨天發生了什麼」，比一天最直接；盤中是拿今天跟一段有代表性的
期間對照，只比一天容易被單日的異常帶走。

為了讓兩邊都是各自的預設，**切換檢視時觀察期間會跟著換成那一邊的預設**，
不把上一個檢視選的期間帶過去（跟排序欄位本來就會歸零是同一個作法）。
另一種選法是讓期間跨檢視沿用，但那樣就沒辦法兩邊同時是預設值。

### 尚未討論

- cron 還是各自寫在兩個 YAML 裡，只靠註解指回 `CollectionSchedule`。
  要不要讓 workflow 直接跟程式問時間（多一個 CLI 指令）還沒談。

---

<a id="todo-7"></a>
## 🟡 7. 盤中成交額準確度：對照已經在背景收，等收滿六天

[↑ 回到 TODO 列表](#快速跳轉)

**狀態：原因、對策、資料來源、盤中即時性全部查證完畢。
對照用的 GitHub Action 已經上線，每個交易日自己收，收滿六個完整交易日再動收集器。**

### 已討論

使用者拿 08/18 收盤後的畫面跟玩股網（<https://www.wantgoo.com/stock/ranking/turnover>）
對照，我們的成交額**每一檔都偏低**：

| 代號 | 名稱 | 我們 | 玩股網 | 比值 | 當日漲跌 |
|---|---|---|---|---|---|
| 2408 | 南亞科 | 430.35 | 445.7 | 0.966 | −1.72% |
| 3037 | 欣興 | 418.53 | 420.3 | 0.996 | +2.23% |
| 2330 | 台積電 | 409.76 | 411.1 | 0.997 | −0.83% |
| 2454 | 聯發科 | 323.89 | 328.0 | 0.987 | −4.07% |
| 2344 | 華邦電 | 319.16 | 332.4 | 0.960 | −2.75% |
| 6182 | 合晶 | 249.51 | 257.4 | 0.969 | −2.04% |
| 3189 | 景碩 | 226.45 | 235.1 | 0.963 | −4.42% |

**偏低是系統性的，不是隨機誤差**，而且跟當日走勢有關：
上漲的（欣興）幾乎沒有差，下跌越多的差越大。

原因幾乎可以確定是現在的算法：

```
盤中成交額 = 現價 × 累計成交量
```

拿**最後一筆成交價**去乘**一整天的量**。股價一路往下走的話，
早盤那些量其實是用更高的價格成交的，用收盤價回乘就會少算——
這正好解釋了「跌越多、差越大」。真正的定義是逐筆成交價 × 逐筆成交量的累加。

`site.js` 裡那句註解說「證交所的盤中介面只給累計量，沒有累計金額」是這個算法的由來。

### 查證結果（2026-08-19 凌晨）

**一、MIS 沒有累計成交金額。** 實際打 `getStockInfo.jsp` 逐欄看過：
只有 `v`（累計成交量，張）與 `z`（最後成交價），沒有任何一個欄位是金額。
2330 回的是 `v=17217`、`z=2380`，`2380 × 17217 × 1000 = 409.76 億`，
跟畫面上那個數字一模一樣——確認算法就是這樣，而且沒有現成的欄位可以換。

順手驗算隱含均價，把「跌越多、差越大」的推論釘死：

| 代號 | 隱含均價 | 收盤 | 當日高低 | 差距 |
|---|---|---|---|---|
| 2330 | 2,387.8 | 2,380 | 2,375 ~ 2,415 | 0.33% |
| 2408 | 533.4 | 515 | — | 3.4% |

隱含均價都落在當日高低之間，合理；跌得越多的那檔差越大。

**二、官方沒有盤中的累計金額可用。**

| 端點 | 有金額嗎 | 盤中會動嗎 |
|---|---|---|
| MIS `getStockInfo.jsp` | ✗ | — |
| MIS `rank.jsp` / `getDealTS.jsp` / `getStatis.jsp` | 已下架，回錯誤頁 | — |
| `openapi.twse.com.tw/.../STOCK_DAY_ALL` | ✓ `TradeValue` | ✗ 盤後才更新，還慢一天 |
| `openapi.twse.com.tw/.../MI_INDEX20` | ✗（只有量，且只有 20 檔） | ✗ |
| `tpex.org.tw/openapi/v1/tpex_mainboard_quotes` | ✓ `TransactionAmount` | 只有上櫃，且看起來是盤後 |

**三、玩股網那份資料是整頁一支 JSON，而且基準跟官方完全一致。**

<https://www.wantgoo.com/investrue/all-quote-info>（`turnover.min.js` →
`_stock-ranking-layout.min.js` 裡的 `$.http.get('/investrue/all-quote-info')`）
一次回 5,859 筆、1.6 MB，欄位有 `millionAmount`（成交金額，百萬元）、
`volume`（張）、`close`、`high`、`low`、`open`、`previousClose`、
`time`（資料時間）、`tradeDate`。

拿 08/18 那一份跟我們 `data/imports/2026-08-18.json` 的官方數字逐檔比：

| 範圍 | 檔數 | 誤差中位數 | p90 | 最大 |
|---|---|---|---|---|
| 全部有成交的 | 1,948 | 0.0205% | 0.60% | 30.7%（都是成交額 0.00 億的殭屍股，`millionAmount` 只到小數兩位＝萬元，捨去誤差被放大） |
| 成交額 ≥ 1 億 | 452 | **0.0009%** | 0.011% | 0.16% |
| ≥ 10 億 | 155 | 0.0004% | 0.007% | 0.019% |
| ≥ 100 億 | 34 | 0.0008% | 0.003% | 0.011% |
| 全市場合計 | — | 我們 11,221.72 億 vs 玩股網 11,221.50 億 → **−0.0019%** | | |

也就是說**它扣掉零股、盤後定價、鉅額的方式跟我們的盤後完全一樣**，
差的只剩 `millionAmount` 進位到萬元。對照現在的 `現價 × 累計量`
（誤差中位數 0.47%、p90 1.72%、最大 6.06%），準確度差了大約 500 倍。
只有 3064 這一檔在玩股網查不到。

### 已確認：盤中真的會即時更新（2026-08-19 開盤後實測，定案）

凌晨打回來的 `time` 是 `2026-08-18 13:30`（上一場的收盤），所以一定要等開盤才驗得出來。
09:00 起每 10 分鐘取樣一次，`dataTime` 一路跟著走，`tradeDate` 也是當天：

| 取樣時刻 | `dataTime` | 落後 | 檔數 |
|---|---|---|---|
| 09:36:59 | 09:37:00 | 0 秒 | 5,842 |
| 09:47:00 | 09:47:00 | 0 秒 | 5,844 |
| 09:57:00 | 09:56:00 | 60 秒 | 5,845 |
| 10:07:01 | 10:06:00 | 61 秒 | 5,845 |
| 10:14:34 | 10:15:00 | 0 秒 | 5,845 |

`dataTime` 只到分鐘，所以「落後」最壞就是一分鐘出頭，對 2 分鐘一輪還在可用範圍內。

**光看數字有沒有變還不夠，所以再驗一次「變的方式對不對」**：
拿相鄰兩次取樣的增量回推均價（Δ金額 ÷ Δ張數），
如果 `millionAmount` 真的是累計成交金額，這個值必須落在那段期間的價格帶裡：

| 區間 | 2330 | 2408 | 2454 |
|---|---|---|---|
| 09:36→09:47 | 2,352.1（報價 2,355） | 491.1（490.5） | 3,871.1（3,875） |
| 09:47→09:57 | 2,351.3（2,350） | 487.7（486.5） | 3,854.2（3,845） |
| 09:57→10:07 | 2,350.1（2,350） | 486.5（484） | 3,848.1（3,845） |
| 10:07→10:14 | 2,349.8（2,350） | 483.4（482） | 3,834.1（3,835） |

每一段都貼著當下的報價，而且 `volume` 與 `millionAmount` 全程單調遞增，沒有一次倒退。
**這條路可以走。**

同一份 10:14 的盤中快照，限縮到我們自己的 1,975 檔股票池，
拿 `millionAmount` 當基準去量現在的算法：

| 指標 | 現價 × 累計量 |
|---|---|
| 誤差中位數（成交額 ≥ 1 億，315 檔） | 0.80% |
| p90 | 2.63% |
| 最大 | 5.88% |
| 全市場合計 | 462,078 vs 466,690 百萬 → **−0.99%** |

比凌晨那份盤後比對（中位數 0.47%）**更差**——合理，因為盤中價離當日均價更遠。
換句話說，盤中正是這個誤差最大的時候，也正是這個畫面要用的時候。

### 實作前要注意的坑（今天實測踩到的）

1. **`id` 會跟指數代號撞號。** 那 5,845 筆裡混了指數：`0000`（加權）、`^658` 之類的類股指數，
   還有 **`2300`、`3000` 這種長得跟股票代號一模一樣的**（close 是兩萬多點的指數值）。
   直接整包吃下去，全市場合計會爆成實際的 184 倍。
   **一定要拿我們自己的股票池去對照著取，不能反過來遍歷對方的回傳。**
2. **涵蓋率 1,974 / 1,975。** 只有 3064（泰偉）查不到，跟凌晨那次一樣。退路要留著。
3. **偶爾會拿到解析不了的東西。** 今天 09:21 那次探針記到 `error: parse`，
   六次裡壞一次。但隨後連打五次都是 HTTP 200、1,674,528 bytes、0.54～0.96 秒、5,845 筆全部正常。
   跟 MIS 那個抖動同一個性質，**照 `MisIntradayClient` 現在的做法就地重試即可**。
4. **一次 request 1.6 MB。** 每 2 分鐘一輪、一天 197 輪約 315 MB，
   比 MIS 那 14 次請求便宜，但別再往下縮短輪距。

### 已經做的：對照用的 GitHub Action（2026-08-21）

「收盤後比一次很準」不等於「整場都撐得住」。動收集器之前要先知道
它會不會中途斷線、會不會有幾檔查不到、會不會某個時段整批停止更新，
所以先開一支**只比對、不改任何現有行為**的背景工作去收證據。

| 東西 | 位置 |
|---|---|
| 指令 | `dotnet run --project src/Invest.Web -- turnover-audit [--loop]` |
| 排程 | `.github/workflows/turnover-audit.yml`，每個交易日 08:11 開跑 |
| 邏輯 | `src/Invest.Web/Infrastructure/TurnoverAudit/` |
| 資料表 | `db/011_turnover_audit.sql`，已套用 |
| 測試 | `tests/Invest.Web.Tests/TurnoverAuditTests.cs`，6 個 |

- 每 5 分鐘一輪（不是收集器的 2 分鐘）：這支只是要看整場的形狀，
  不必跟收集器一樣密，也不必每輪都多打 MIS 那十四個請求去加重對方負擔。
- 兩邊的快照**同一輪內併發抓**，否則差的是時間不是算法。
- 只存摘要與離群值，不存全市場逐檔：逐檔六天約 65 萬列，摘要不到 2 萬列。
- 不加總對方的全部回傳（見下面第 1 點的坑），只拿我們的股票池去查對方。
- 前端完全不碰這兩張表，所以不開 anon。

**收滿 `TurnoverAuditStore.RequiredDays`（6 個完整交易日）、下面「要做的」做完之後，
上表那五樣東西連同 `Program.cs` 裡的指令與 DI 註冊一起刪掉**，
`db/011_turnover_audit.sql` 檔頭有刪表的指令。
進度用不帶 `--loop` 的同一個指令看，它會印每個交易日收了幾輪、還差幾天。

2026-08-21 21:35 的煙霧測試（收盤後，所以那一列不算完整交易日）：
對到 1,971 檔、查無 1 檔，合計 8,670 億 vs 8,687 億（−0.20%），
逐檔中位數 0.51%、p90 2.05%、最大 25.00%，一輪 8.8 秒。

### 要做的

1. 盤中收集器每輪**多打一次玩股網**（一次 request 就整個市場，比 MIS 便宜）。
2. 成交金額改以 `millionAmount` 為準；玩股網掛掉或查無該檔時，
   退回現在的 `現價 × 累計量`。哪一種來源要記在 `intraday_quotes` 裡，
   跟現有的 `IntradayPriceSource` 同一個精神——事後查得出來每一筆是怎麼算的。
3. 價格、漲跌、五檔仍然以官方 MIS 為準，不換。

### 尚未討論

- 依賴第三方網站的風險：對方沒有公開 API 的承諾，版面或路徑改了就會壞。
  所以第 2 點的退路一定要留著，不能拿掉。
- 退路本身還可以再變好：我們每 2 分鐘存一次累計量，
  兩輪之間的**增量 × 那段期間的價**累加起來就是自己算的 VWAP，
  比單一收盤價回乘準得多，而且完全不靠外部網站。
  但 `intraday_quotes` 現在**沒有存累計量**，要先加欄位。
  這件事等玩股網那條路確定了再決定要不要做。
- 盤後那一份成交值是官方原始資料，**沒有這個問題**，不要一起改。

---

<a id="todo-8"></a>
## 🟡 8. 已合併的遠端分支要不要刪

[↑ 回到 TODO 列表](#快速跳轉)

（原本記在 `Doc/完成進度.md` 的「還沒做的」，那個段落已依你的要求刪掉，搬來這裡。）

### 已討論

- `origin` 現在除了 `main`／`data`／`gh-pages` 之外還留著 8 支分支（2026-08-21 實查）：

  | 分支 | 內容是否已在 main |
  |---|---|
  | `codex/custom-data-view` | 已併入 |
  | `codex/custom-data-view-docs` | 已併入 |
  | `codex/intraday-index-kline-release` | 已併入 |
  | `codex/kline-color-release` | 已併入 |
  | `codex/revenue-history-docs` | 已併入 |
  | `codex/revenue-history-release` | 已併入 |
  | `codex/update-release-documentation` | 已併入 |
  | `agent/fix-intraday-collection` | commit 沒併入，但**三項改動在 main 都已存在** |

- `agent/fix-intraday-collection` 特別確認過：個股清單重試、`SecurityCatalog` 資料庫備援、
  `catch (OperationCanceledException) when (cts.IsCancellationRequested)`，
  在目前 `main` 全部找得到（`StockUniverseClient.cs:62`、`Program.cs:358`）。
  所以它是「內容重覆」而不是「有東西還沒進來」。
- **刪遠端分支要你點頭才做。** 這是不可逆的，而且沒有急迫性，留著只是列表有點長。

### 尚未討論

- 要不要一次刪掉這 8 支，還是只刪 `codex/*` 那 7 支、`agent/*` 再留一陣子。

---

<a id="todo-9"></a>
## 🟡 9. 盤中「成交比變化」疑似每列都是 +0.01%

[↑ 回到 TODO 列表](#快速跳轉)

（同樣是從「還沒做的」搬過來的，**還沒查證**。）

### 已討論

- 現象：盤中把比較期間設成只比一天時，「成交比變化」整欄看起來都是 `+0.01 %`。
- 只在盤中看到過一次，**沒有留下截圖或數字**，所以先當作觀察，不當作 bug。
- 讀過程式：`site.js` 的 `row.shareChange = row.share - past.share`，
  分母 `marketTotal` 是當下這一輪的全市場合計，對照組是最新一個交易日的盤後同一欄。
  公式本身沒問題，兩邊也都是「佔全市場的比例」，定義對得起來。
- 可能的解釋（都還沒驗）：
  1. 盤中只跑到一半，全市場合計比整天小，每一檔的佔比會被整體推高一點點——
     如果是這個原因，那**不是 bug，是定義使然**，但至少要在畫面上講清楚。
  2. 顯示格式把很小的差值都進位到 `+0.01 %`。
  3. 對照組真的抓錯期間。

### 尚未討論

- 要等下一個交易日盤中打開畫面，先確認整欄是不是真的一模一樣，
  再決定是修顯示、修定義，還是根本不用動。**盤後那一欄不受影響，不要一起改。**

---

<a id="todo-10"></a>
## 🟡 10. 盤中輪距要不要縮到 1 分鐘

[↑ 回到 TODO 列表](#快速跳轉)

**狀態：以目前的資料來源不建議，綁在第 7 項後面重新評估（筆記 #13，2026-09-05 用正式資料庫再次驗證同一個結論，維持 pending）**

### 已討論（2026-08-23 量測）

問的是「盤中資料更新速度能不能縮短至 1 分鐘」。拿 2026-08-21 那一整場
（09:00–13:30）實際收到的輪次回頭算：

| 指標 | 實測 |
|---|---|
| 收到的輪次 | 125 |
| 平均輪距 | 130 秒 |
| 最長輪距 | 240 秒 |
| 被跳過的格子 | 10 格（7.4%） |

跳過的時刻：09:07、09:15、09:59、10:18、11:05、11:31、11:41、12:34、12:53、13:17。

**縮短輪距反而會讓資料更舊。** `MisIntradayClient` 把單輪的 HTTP 預算
綁在 `CollectionSchedule.IntradayInterval` 上，輪距減半、預算也跟著減半。
一輪是十四次請求、實測 12～75 秒（中位數 36 秒），2 分鐘的預算已經會掉 7.4%；
1 分鐘的預算會把中位數以上的輪次全部丟掉，畫面上看到的是「更常沒有更新」。
順帶一提對 MIS 的請求密度也會從 7 req/min 變成 14 req/min，被擋的風險跟著上升。

**所以先修的是另一頭。** 使用者回報「用手機看有時候超過 7 分鐘沒更新」，
但收集端最壞只到 4 分鐘——差額出在前端：手機把分頁凍住時 `setInterval`
整個停擺，解凍後是「接著跑」不是「補跑」。這件事已經修掉（見
[Doc/完成進度.md](Doc/完成進度.md)），實際感受應該會直接改善。

### 2026-09-05 用正式資料庫再驗一次（筆記 #13）

筆記 #13 原本卡的點是「CDN 還沒上線」，但這個卡點已經解除：`intraday-snapshots`
bucket 已上線且有健康度監控，`site_alerts` 近期沒有 CDN 相關警報。可是重新拿
`intraday_curve` 量 09:00–13:31 每一輪的實際間隔，翻出比 CDN 更硬的阻礙——
**現在連 2 分鐘都跟不上**：

| 日期 | 輪數 | 準時（≤2.5 分） | 落後 3–4 分 | 落後 >4 分 | 最長斷點 |
|---|---|---|---|---|---|
| 09-01 | 114 | 97 | 14 | 3 | 360s |
| 09-02 | 106 | 85 | 15 | 6 | 480s |
| 09-03 | 125 | 117 | 7 | 1 | 360s |
| 09-04 | 119 | 108 | 8 | 3 | 360s |

每天已經有 9～20% 的輪次沒跟上格子。而且單輪超時預算目前直接等於間隔
（`MisIntradayClient.cs:55` `budget.CancelAfter(CollectionSchedule.IntradayInterval)`），
改成 1 分鐘會把預算從 120 秒砍成 60 秒，但單輪實測要 12～75 秒（中位數 36 秒）——
最慢那些輪會直接被砍掉、整輪近兩千檔全部作廢。**結論不變：維持 2 分鐘。**
重啟條件是先把「超時預算」與「間隔」解耦、並讓單輪穩定在 30 秒內，才回頭談縮短。

### 重新評估的條件

第 7 項換成玩股網那條路之後，一輪從**十四次 MIS 請求**變成**一次 request**，
單輪耗時會從中位數 36 秒掉到個位數秒。那時候 1 分鐘的預算才可能撐得住，
再回來量一次同樣這四個數字。

---

<a id="todo-11"></a>
## 🟡 11. 換發布網址，讓訪客查不到原始 GitHub 帳號

[↑ 回到 TODO 列表](#快速跳轉)

**狀態：已決定收掉 `admin888/`、`viewer/` 這兩個過渡網址，改成單一根目錄網址
＋登入分層（訪客為預設，監控者／最高權限需登入）；程式已 commit 並推送
`main`，且已由 publish-only 流程發布，正式網站目前使用
`frank-invest.github.io/` 根目錄（見下方「收攏成單一網址」一節）；`qwe953751.github.io/Investment/`
根目錄維持空白頁**

### 已討論（2026-08-24）

最早的需求是買 `frank-investment.com` 網域換掉 `qwe953751.github.io/Investment/`，
但查到這個網域沒人註冊，你也明講不想花錢買網域，所以那條路直接放棄。

你把真正的目的講清楚之後（「讓使用此網站的訪客，查不到原始作者的 GitHub」），
改用不花錢的作法：**建一個新的 GitHub 組織**裝發布內容，而不是改帳號名稱或買網域。
你已經建好組織 `frank-invest`（https://github.com/frank-invest）。我接著做完：

- 建立 `frank-invest/frank-invest.github.io` repo（GitHub 的特殊命名規則：
  repo 名稱等於帳號/組織名時發布在網址根目錄），發布出來就是
  **`https://frank-invest.github.io/`**——不含 `qwe953751`，也不含 `/Investment`
  路徑。檢視權限沿用既有機制，網址加 `?access=viewer` 即可，不需要另外的網址。
- 修掉一個原本就存在、跟這次目的直接衝突的洩漏：`scripts/publish-gh-pages.sh`
  發布用的 commit 原本吃本機 `git config` 的身分（`frank <qwe953751@gmail.com>`），
  這個 email 在 GitHub 帳號上是已驗證的，訪客點開 repo 的 commit 紀錄，即使
  作者名字打的是「frank」，頭像跟連結還是會指回真實帳號。已經改成固定用
  `github-actions[bot]` 身分，不管是本機手動發布還是 CI 自動發布都一樣，
  無論發到哪個 repo 都不會洩漏。
- `daily-snapshot.yml` 加了一步「發佈到 frank-invest.github.io」，跟原本發布到
  `qwe953751.github.io/Investment/` 那步並列，每天兩邊都會自動更新，不用手動同步。
  推送用的 `FRANK_INVEST_PAT`（存在 `qwe953751/Investment` repo secret）是我本機
  `gh` CLI 那組個人 token，因為新 repo 在別的 org 底下，CI 內建的
  `GITHUB_TOKEN` 天生跨不過去。**這組 token 如果你之後在本機重新登入 `gh` 導致
  舊 token 失效，這一步會開始紅燈**，到時候要重新 `gh secret set FRANK_INVEST_PAT
  --body "$(gh auth token)" --repo qwe953751/Investment` 補一次。
- 已實測確認沒有洩漏：組織的公開 People 頁面（GitHub 預設把成員身分設成
  private，目前是空的）、repo 首頁、commits 頁面、org 的 repo 列表頁，直接
  curl 抓 HTML 找不到 `qwe953751` 字樣；`gh-pages` 分支最新 commit 作者是
  `github-actions[bot]`，不是真實帳號。

### 你回報「People 那個頭像點進去是我的 GitHub」（2026-08-24 已釐清）

你傳的截圖裡有「Create new repository」「Invite someone」這兩個按鈕，
還有一個點進去是 `qwe953751` 的頭像——**這是你自己登入 GitHub 之後看到的
org 管理畫面**，不是訪客看到的樣子。那兩個按鈕本來就只有組織的擁有者/成員
登入後才看得到，訪客（沒登入，或登入的是別的帳號）看不到這個 People 卡片，
也看不到你的頭像。

用完全匿名的 `curl`（不帶任何 cookie）重新驗證過：`https://github.com/frank-invest`
的原始碼裡沒有 `qwe953751` 字樣、沒有任何連到 `/qwe953751` 的連結，唯一出現的
頭像是組織自己的 logo（`avatars.githubusercontent.com/u/320591447`，這是
`frank-invest` 這個組織的頭像 id，不是你的帳號 id `17944469`）。**你想親眼
確認的話，開一個無痕視窗（或先登出 GitHub）再看一次 `github.com/frank-invest`，
應該看不到 People 卡片跟那兩個管理按鈕。**

### 檢視權限的獨立網址（2026-08-24 已補上）

你問「說好的兩個網址呢」——原本規劃檢視權限只用同一個網址加
`?access=viewer` 參數，沒有另外給一個好記、能單獨分享的網址。已經補上：

**`https://frank-invest.github.io/viewer/`**

做法不是複製一份全站資料（那樣每次發布要多推 300+MB），而是在 `viewer/`
資料夾放一個幾行字的轉址頁，進去就用相對路徑 `../?access=viewer` 跳回上一層、
帶上檢視權限參數。因為是相對路徑，同一支 `publish-gh-pages.sh` 不用改就能同時
用在根目錄發布（新網址）跟子路徑發布（舊網址 `qwe953751.github.io/Investment/`），
兩邊都會自動有一份：

- 最高權限：`https://frank-invest.github.io/`
- 檢視權限：`https://frank-invest.github.io/viewer/`

這個轉址是「同網域內跳轉」，不是 iframe：瀏覽器網址列會確實換成
`.../?access=viewer`，原始碼裡也不會出現另一個網域的網址——跟下面提到的
`Investment-view` 的 iframe 做法（原始碼直接寫著要嵌哪個網址）不一樣，
不會有洩漏問題。

### 最高權限改發到 admin888 子路徑、兩邊根目錄都收掉內容（2026-08-24 已完成）

你進一步要求：(1) 最高權限網址從 `https://frank-invest.github.io/` 改成
`https://frank-invest.github.io/admin888/`；(2) 舊網址
`https://qwe953751.github.io/Investment/` 跟新網址根目錄
`https://frank-invest.github.io/` 兩個都要看不到內容。已經做完：

- `scripts/publish-gh-pages.sh` 改成**網域根目錄一律只發一頁空白頁**（`noindex`、
  沒有任何排行資料），真正的內容改由新的 `GH_PAGES_ADMIN_SUBDIR` 環境變數指定
  要放進哪個子路徑；沒設這個變數時，整個網域只有空白頁，什麼都不發。
- `daily-snapshot.yml` 的兩個發布步驟分工：
  - 舊網址（`qwe953751/Investment` 的 `gh-pages`）不設 `GH_PAGES_ADMIN_SUBDIR`，
    只發空白頁——等於這一步已經不是「收掉」而是「留著 Pages、但打開網址
    什麼都看不到」，不用去 repo 設定關 Pages。
  - 新網址（`frank-invest/frank-invest.github.io`）設
    `GH_PAGES_ADMIN_SUBDIR=admin888`，最高權限變成
    **`https://frank-invest.github.io/admin888/`**，根目錄
    `https://frank-invest.github.io/` 只剩空白頁；檢視權限維持獨立、好記的
    **`https://frank-invest.github.io/viewer/`**（跟 `admin888/` 平級，不是巢狀
    在它底下——這是你點出我第一版做錯的地方：我原本把 viewer 也搬進
    `admin888/viewer/`，等於檢視權限網址被迫跟著改名，你要的其實是「最高權限
    换路徑，檢視權限網址不變」），進來會轉址到 `admin888/?access=viewer`。
- 本機用假的 bare repo 當 remote 跑過一次真的 `publish-gh-pages.sh`，確認兩種
  模式各自產出的檔案結構符合預期（空白模式只有 `index.html` + `.nojekyll`；
  admin888 模式根目錄空白、`admin888/` 底下有完整站台、`viewer/` 在根目錄
  跟 `admin888/` 平級），再進 CI 實際發布一次收尾。
- `README.md`、`AGENTS.md`、`Doc/開發環境.md` 裡引用舊網址當「正式網址」或
  拿舊網址驗證發布結果的地方一併同步，避免其他裝置照著舊文件驗證時打到
  已經清空的網址。

### 檢視網址改用 iframe，不再用整頁轉址（2026-08-25 已完成）

你用筆記回報一個 Bug（`[權限]分網址`）：分享「訪客網址」
`https://frank-invest.github.io/viewer/` 出去，對方打開後網址列會變成
`https://frank-invest.github.io/admin888/?access=viewer`——把本來要藏的最高
權限路徑洩漏出去了，違背當初設 `admin888` 這個路徑的目的（「不讓人有機會去
操作開發者權限的東西」）。原因是 `viewer/index.html` 原本用
`location.replace('../admin888/?access=viewer')` 做**整頁轉址**，網址列本來就
會確實換成目標網址。

改法：`scripts/publish-gh-pages.sh` 的 `viewer/index.html` 改成用 **iframe**
內嵌 `admin888/?access=viewer`，網址列全程停在 `.../viewer/`，不會再換成
`admin888/...`。也考慮過整份複製一份內容到 `viewer/`（讓 `site.js` 內建的
`ACCESS_PATH === 'viewer'` 判斷直接鎖死，不靠網址參數），但 `admin888/`
底下光 `data/` 就有三百多 MB，兩份會逼近 GitHub Pages 每個網站 1GB 的軟
上限、還會隨交易日持續變大，放棄。iframe 的 `src` 刻意用 JS 在頁面載入後
才設定、不寫死在靜態 HTML 裡，所以 view-source（看原始碼、不執行 JS）也
看不到 `admin888` 字樣，只有主動開 F12 開發者工具才挖得到。

仍要說清楚：這**不是**真正的存取控制，只是不讓「分享網址」這個動作本身
洩漏路徑。`admin888/?access=viewer` 這個網址一旦被挖出來（例如開發者工具
或原始碼查得夠仔細），拿掉 `?access=viewer` 一樣能看到最高權限畫面——這個
專案從一開始就沒有登入機制，跟其他地方（`GH_PAGES_ADMIN_SUBDIR`、
`?access=viewer`）的風險等級一致。本機用假的 bare repo 重跑過
`publish-gh-pages.sh`，確認 `viewer/index.html` 產出的 iframe `src` 正確
帶出 `../admin888/?access=viewer`。

### 根目錄復原訪客模式、登入列改版面（2026-09-01，已 commit、推送並發布）

筆記 #37 的密碼登入三層權限上線後，子路徑（`admin888`）不再是唯一防線——訪客／
監控者／最高權限已經看登入狀態決定，所以你要求把 `frank-invest.github.io/`
根目錄從「刻意留空白頁擋人」復原成訪客模式。已做完：

- `scripts/publish-gh-pages.sh` 新增可重用的 `write_iframe_page()`，根目錄跟
  `viewer/` 現在都用同一招同網域 iframe 轉發頁，內嵌 `admin888/?access=viewer`，
  網址列不會出現 `admin888` 字樣。只動有設 `GH_PAGES_ADMIN_SUBDIR` 的發布目標
  （`frank-invest.github.io`），舊網址 `qwe953751.github.io/Investment/` 沒有
  設這個變數，維持原本的空白頁不受影響，不在這次復原範圍內。
- 轉發頁會把自己網址列的 query string（例如 `?key=密碼`）轉貼進 iframe 的
  src，所以長者友善連結之後可以直接分享 `.../viewer/?key=<密碼>`，不用先進
  `admin888/`。
- 順便把登入列（`#access-bar`）從「頁首最上面單獨一整條」改成併入標題那一排、
  推到右上角，視窗窄於 720px 時跟原本一樣整條換到下一行。
- 本機用假 remote 跑過 `publish-gh-pages.sh` 驗證產出內容，也用真實資料本機
  預覽過新版面；publish-only run `33474489830` 已完成輸出與發布，公開根目錄與
  `viewer/` 已驗證為 iframe 轉發頁。

### 收攏成單一網址，admin888／viewer 收掉（2026-09-01，已 commit，已發布）

登入分層（筆記 #37）上線後，`admin888` 子路徑跟 `viewer/` 轉發頁已經不是真正
的存取控制，只是不讓網址列直接暴露一段字串，還多一層 iframe 轉發要維護。
你確認方向後直接收掉這兩個網址，改成單一根目錄網址＋登入分層：

- 最終網址：**`https://frank-invest.github.io/`**（單一網址，預設訪客，
  監控者／最高權限一律要密碼登入才能拿到）。
- 長者友善連結：**`https://frank-invest.github.io/?key=<監控者密碼>`**，延續既有
  `?key=` 自動登入機制（`AUTOLOGIN_QUERY`），開頁即自動登入監控者模式，沒有另外
  再做轉發頁。
- `site.js`：`URL_ACCESS` 從「不是 viewer 路徑就預設 admin」改成一律預設
  `'viewer'`；清掉沒用過的 `ADMIN_HOST`／`VIEWER_HOST`／`DEPLOYED_ACCESS` 自訂
  子網域死碼。本機測試仍可用 `?access=admin`／`?access=viewer`（限 localhost）
  覆寫，不受影響。
- `scripts/publish-gh-pages.sh`：拿掉 `write_iframe_page()` 與
  `GH_PAGES_ADMIN_SUBDIR` 的子路徑＋iframe 轉發殼，改用新的布林旗標
  `GH_PAGES_PUBLISH_ROOT`——設定時內容直接發到網域根目錄，不設定時維持原本的
  空白頁行為（`qwe953751.github.io/Investment/` 不受影響，不在這次範圍內）。
- 同步調整 `daily-snapshot.yml` 發布步驟名稱與環境變數、`intraday.yml` 的
  `SITE_MANIFEST_URL`、`Program.cs` 的回補用 `ManifestUrl`、`AGENTS.md` 的
  發布後驗證指令、`README.md`／`Doc/開發環境.md` 的網址說明，全部從
  `admin888/manifest.json` 改成根目錄 `manifest.json`。
- 測試同步：`DailySnapshotWorkflowTests.cs`、`StaticKLineAssetTests.cs` 裡斷言
  舊字串（發布步驟名稱、`ADMIN_HOST`／`VIEWER_HOST`）的地方改成新字串。
- 本機驗證：`dotnet test` Release 全數通過（`331/332`，唯一失敗是另一個並行
  session 尚未完成的 OCR 工作，跟這次改動無關）；`publish-gh-pages.sh` 用本機
  假 remote（`/tmp` bare repo）分別跑過 `GH_PAGES_PUBLISH_ROOT=1` 與不設定兩種
  模式，確認根目錄直接拿到完整網站內容、未設定時維持純空白頁。
- **狀態**：程式碼已 commit 並推送 `main`，`daily-snapshot.yml` publish-only run
  `33890231986` 已成功發布；正式網站已切換成 `frank-invest.github.io/` 單一根目錄網址。

### 尚未做的

- `qwe953751/Investment-view`（`https://qwe953751.github.io/Investment-view/`）
  這個獨立 repo 用 iframe 內嵌 `qwe953751.github.io/Investment/?access=viewer`，
  iframe 的 `src` 網址直接寫在頁面原始碼裡，等於還是會洩漏舊網址（進而洩漏帳號）。
  舊網址現在只剩空白頁，所以這個 iframe 實際嵌進來的畫面也會是空的，但原始碼裡
  那條 `src` 還是看得到——漏洞的「洩漏帳號」部分沒有解決，只是「洩漏內容」的
  部分因為舊網址本身已清空而自然失效。這個 repo 的用途已經被
  `frank-invest.github.io/viewer/` 取代，等你確認新網址沒問題後，這個
  repo 也應該一併收掉或改成轉址，不然它會是另一個漏洞——目前還沒有動它，先留著
  等你一起決定。
- `app.admin` / `view.frank-investment.com` 那組子網域規劃（含各自要不要
  獨立的 GitHub Pages 來源）已經不需要了，除非你之後改變主意想要自訂網域
  （例如想要更好記的名字），需要的話再重談。

---

<a id="todo-12"></a>
## 🟡 12. 新聞熱度目前在量「節點多大」而不是「題材多熱」，要基準線才修得掉

[↑ 回到 TODO 列表](#快速跳轉)

**狀態：等資料（要能算出「這個族群平常一個月發幾則」的基準線，
而基準線要有歷史；`material_events` 目前只累積 8 天）。**

### 已討論

新聞熱度已經按研究文件 §12 做出來了（`TopicNewsHeatCalculator`），
公式是材料性 × 新鮮度 × 依催化類型的時間衰減，加總後做指數飽和
`100×(1−e^(−Σ/3))`。你拍板的是**先擺參考欄，別動熱度數字**，所以它現在
只顯示在「新聞熱度（參考）」那一欄，權重固定 0，沒有進綜合熱度。

不併入不只是因為「還沒校正」，是因為第一次跑真實資料就量得出一個具體的偏誤：

| 族群 | 成員數 | 新聞熱度 | 資金熱度 |
|---|---:|---:|---:|
| 傳產 | 253 | 99.4 | 37.7 |
| 金融保險 | 15 | 97.0 | — |

Σ 分數跟成員數的相關係數是 **0.77**。節點大就一定有人在發公告，
所以這個分數目前有一半在量「這個節點有多大」。
金融保險是反例——只有 15 檔卻拿到 97 分，那是真的一直在發
（財報、法說會、金控併購），這種才是我們要的訊號。

**現在把它加進加權會發生什麼**：全站每一個族群的熱度數字跟排序都會變，
而且大節點會系統性地被推上去，畫面上不會有任何錯誤訊息。
`TopicHeatCalculatorTests.新聞熱度只填進欄位不會動到綜合熱度` 就是為了這個
存在的——誰順手把它接上加權，測試會當場紅燈。

### 尚未討論

**修法的方向大致清楚，但參數要資料才定得下來。**
把絕對分數換成相對基準線：算出每個節點過去 N 個月「平常一個月發幾則、
加起來幾分」，實際分數除以（或減去）基準線，超出基準才叫熱。
這樣 253 檔的傳產平常就發很多，基準線也高，不會因為體積大就永遠滿分。

需要決定、但現在談不下去的：

- **基準線要看多久**：3 個月？6 個月？太短會被單一事件汙染，
  太長對新成立的族群節點沒有意義。要有資料才試得出來。
- **要用比值還是差值**：比值（實際/基準）對小節點太敏感，一則公告就翻倍；
  差值（實際−基準）又會讓大節點的絕對量還是佔優。可能要 z-score。
- **併入之後那一欄要不要改名**：現在前端靠 `NewsWeight == 0` 判斷欄位要叫
  「市場熱度」還是「綜合熱度」，真的併入時那一欄會自動變成「綜合熱度」，
  權重也會從 0.7059:0.2941 回到文件原本的 0.60:0.25:0.15。這是設計好的，
  但要先確認那個 0.15 是不是還合理。

還有一個更根本的限制：**目前唯一的來源是公開資訊觀測站的重大訊息**，
所以 §12 五項裡的「來源可信度」跟「獨立來源確認」都退化成常數 1.0。
真正的「多個獨立來源互相佐證」要等媒體來源接上才有意義，
在那之前這個分數本質上是「這個族群的公司最近公告了什麼」，不是「市場在談什麼」。
要不要接媒體來源、接哪一家，是另一件事，見研究文件 §14。

---

<a id="todo-13"></a>
## 🟡 13. GitHub 排程事件晚到 6～13 小時，自動收集與每日快照都可能整天沒跑

[↑ 回到 TODO 列表](#快速跳轉)

2026-08-28 從筆記 #27 查出來的。**這一項不是我們的程式壞掉**，
是 GitHub 那邊沒有把 `schedule` 事件按時送過來。

### 已經查證的

以 Actions API 比對每個 run 的 `created_at` 與 `run_started_at`，兩者相同，
所以事件是**晚送達**，不是送到了在排隊等 runner。

- **08-27**：一整天沒有任何 scheduled 的盤中 run。當天所有 run 都是 `workflow_dispatch`。
- **08-28**：三個盤中 cron（台北 07:33／08:17／09:01）分別在 15:01、19:22、20:30 才送到，
  全部落在收集時段外，被 `preflight` 正確略過——也就是說當天自動收集等於沒跑。
  同一天 18:00 的每日快照，連同 CodeX 加的兩個備援（18:17、18:47），**完全沒有送達**。
- **08-27 那場**每日快照的事件在隔日台北 03:02 才到，原本的「睡到 18:00 再開抓」
  會直接吃掉 runner 六小時硬上限被 cancel，所以 CodeX 加了 preflight 擋掉早到太久的事件。
- 08-27 之前，同一組 cron 一直只誤點 15～30 分鐘，是可以接受的範圍。

排除掉的可能：repo 是公開的（Actions 不計費）、Actions 是啟用狀態、
沒有排隊中的 run、runner 用量自 08-18 起穩定在每天 7～12 小時。
沒有任何 in-repo 的變更能解釋這個轉折。

### 解法：不再等 GitHub 的鬧鐘（2026-08-29，519e53a0）

原本這一節寫的是「加更多 cron ＋ 前端橫幅，剩下的等 GitHub 恢復」。
那不算修好——每次開盤沒更新都得找人遠端補跑。已經改掉。

**先推翻一個我自己的錯誤判斷。** 我原本認定 `GITHUB_TOKEN` 觸發的事件不會再觸發
別的 workflow，所以排程只能靠 GitHub 自己的鬧鐘。實際上 `workflow_dispatch` 與
`repository_dispatch` 是那條遞迴保護的**例外**，文件明寫這兩種事件一定會產生 run。
已實測：拋棄式 workflow 用 `GITHUB_TOKEN` 連續自我觸發三棒
（run 33177347324 → 33177357163 → 33177365236），三場全成功、間隔 6 秒。

於是 `intraday.yml` 改成**自走鏈**：

- 每一棒開跑的第一件事就是把下一棒 dispatch 起來排隊，然後才 checkout。
  放第一是因為這一棒可能撞到 6 小時硬上限、runner 可能掛掉、可能被 cancel，
  那些情況都輪不到最後一步執行。
- 自己算出下一次開盤（跳過週六日）睡過去，時間到就收集。
- `concurrency: intraday-daemon` 保證同時只有一棒在跑、只有一棒在排隊。
  新的 pending 會取代舊的 pending，所以不會愈叫愈多。
  排程與手動共用同一組鎖——分開就等於允許兩個收集器並行寫同一輪。
- 撐不完整場的那一棒不硬收，改當佔位棒，提早 10 分鐘退場，
  把 checkout 與 .NET 安裝的暖機時間讓給下一棒。
- 每一棒至少活 3 分鐘，避免哪天每棒都秒失敗變成一天上千個 run。
- 移除 `preflight`：它會丟掉晚到的事件，正是 08-28 沒資料的原因之一。
  自走鏈的每一棒都自己重算下一次開盤，事件晚到 13 小時也能接回來。

cron 保留但降級成**復原火種**，另外多加一發週末的（週五盤後斷鏈時，
中間沒有平日 cron 可以接）。順便讓這條鏈兼任每日快照的鬧鐘：
平日過了 18:00 而當天還沒有成功的快照就把它叫起來。

**代價**：長期佔著一個 runner。repo 是公開的、Actions 不計費、
併發上限 20 個 job 只用 1 個，可以接受。

**要立刻重跑一輪時不要另外觸發**（會排到 5 個多小時後），
直接把正在睡的那一棒 cancel 掉——下一棒已經排在後面，會馬上頂上重新判斷。

### 已經評估並否決的其他做法

- **加更多 cron**：沒有用。08-28 三個 cron 全都晚到，不是漏掉其中一個。
- **`workflow_run` 自我循環**：GitHub 把鏈長限制在 3 層，接不成永動的迴圈。
- **Supabase `pg_cron` + `pg_net` 去打 GitHub API**：實測可行
  （擴充功能已啟用，`net.http_get` 回 200），但要多一把會過期的權杖、
  多一個要維護的東西。使用者要求「不要靠任何裝置、GitHub 自己處理掉」，否決。
  → 那兩個擴充現在沒用到，之後可以考慮移除。
- **外部 cron 服務／自己的機器**：同上，且公司電腦有網域限制、不是隨時在。

### 上線時踩到的坑（已修，並補了測試釘住）

Actions 的預設 shell 是 `bash -e`，errexit 是命令列給的，
步驟裡寫 `set -uo pipefail` **關不掉它**。等待迴圈用「條件不成立就 return」的寫法，
裸 `return` 會把上一個判斷的狀態 1 傳出去，於是整個步驟被 errexit 打掉，
每一棒都在 20 秒內失敗。那個「每棒至少 3 分鐘」的下限剛好把暴衝擋在一小時 20 棒。
已明確 `set +e`，並把所有「現在不必做這件事」的 return 釘成 `return 0`。

### 08-31 開盤驗收：兩個新成因，都已修

第一個（盤中，commit `89e0f7b4`）：`concurrency` 是**整個 run** 層級的鎖。
`hop` 提早交棒收工後，同一個 run 裡的 `publish-manifest` 還在等 CDN（上限五個半小時），
run 一直算 `in_progress`，排隊中的下一棒從 08:30 一路 pending 到 10 點多，
中間正好蓋掉開盤。已改成 `publish-manifest` 每輪順便查 `hop` 收工了沒，收工就放鎖。
08-29 的假時鐘只推 `hop` 那條路徑，沒有 `publish-manifest`，結構上就驗不出這件事。

第二個（盤後，commit `b2e97a63`，筆記 #45）：當天 `daily-snapshot.yml` 的三發 cron
**一發都沒送到**，全靠這個鬧鐘補位，結果鬧鐘自己判錯。
舊寫法只看 run 的 `conclusion == "success"`，但 `publish-only=true` 的純發布
會**跳過「回補行情」**、只重跑輸出與發布，`conclusion` 一樣是 `success`。
當天白天手動發布過網站 10 次，鬧鐘在台北 18:00:30 印出
「今天已經有成功的每日快照，不重複叫」，整天沒有人去收盤後行情，
`data` 分支停在 `2026-08-28`。已改成翻開當天每個 run，
確認「回補行情」這一步真的 `success` 過才算數。

**留下來的通則**：run 成功不等於資料進來了。判斷「今天做完了沒」要看做事的那一步，
不要看 run 的結論。

### 仍然保留的那一層

前端「這不是今天的資料」橫幅照舊留著。自走鏈把「排程沒來」這個成因解掉了，
但收集器本身還是可能因為別的原因沒跑成，橫幅是最後一道讓人當下看得出來的防線。

當天缺的資料一律用 `workflow_dispatch` 補跑真正的收集器補回來，
不補假資料，也不改資料庫裡既有的交易日。

---

<a id="todo-14"></a>
## 🟡 14. Supabase 流量超額，9/27 起適用 Fair Use Policy

[↑ 回到 TODO 列表](#快速跳轉)

筆記 #30。Supabase 已寄出超額通知：Egress 6.522 / 5 GB（130%）。

### 硬期限

**2026-09-27 起 Fair Use Policy 生效，要把 egress 壓到 5.5 GB 以下**，
否則可能被限制服務。寬限期內若持續大幅超標，官方保留提早限制的權利。

### 已查證的

- **成因確定是盤中全市場讀取。** 8/28 有 4,782 次 REST 請求，若其中一半是那個
  165 KB 的全市場回應，約 396 MB／天、20 個交易日約 8 GB／月——跟看到的
  6.522 GB 同一個量級。不是筆記、資產或其他功能造成的。
- **egress 從 2025 年 8 月起是兩個各自獨立的額度**，Free Plan 兩邊都是 5 GB：
  uncached（Database REST、Auth）與 cached（Storage 的 CDN 命中）。
  **快取命中不是免費的，只是比較便宜。**
- **Smart CDN 已經自動壓縮**（實測 9.3 倍）。不需要也不可以自己先 gzip 再上傳：
  storage-api 會丟掉 `content-encoding`，自己壓會讓瀏覽器收到亂碼。
- **Management API 查不到位元組數**（`/usage`、`/billing/usage`、`usage.api-egress`
  全部 404），只開放請求次數。位元組只有 Dashboard 看得到。

### 已經做的

盤中全市場讀取改走 Supabase Storage 的公開 CDN，詳見
[Doc/技術文件/Supabase多裝置流量節流與盤中CDN決策.md](Doc/技術文件/Supabase多裝置流量節流與盤中CDN決策.md)。
`publish-manifest` 會自動監看有沒有掉回資料庫直連並報警（同上文件 §7）。

### 還沒定案的：計費算壓縮前還是壓縮後

官方文件沒寫。幾乎確定是在 CDN 邊緣量測（壓縮後），但在量到之前不當成已知。
兩種情況的容量差很多：

| 計費基準 | 全天裝置容量 |
|---|---:|
| 壓縮後（大概率） | 約 28 台 |
| 壓縮前（最壞） | 約 5.5 台 |

實際只有 3 台而且不會全天開著，連最壞情況都有餘裕。

**8/31 收盤後拿真實流量對 Dashboard 的 Cached Egress 比對，用量測定案，不要用猜的。**
跑一天就算用最差的計費方式也只有約 1 GB，離 5 GB 還很遠，量得起。

### 手上還沒打的牌

1. **表示法壓縮**（無損，全市場個股都還在）：改位置陣列去掉重複欄位名 1.25×；
   再去掉 `name`（前端有代號對照表）1.7×。
2. **tier-1 搬到 GitHub Pages**：`revenue_latest`、`revenue_history`、`market_flags`、
   `site_alerts` 這 4 支是「每個人都一樣、日粒度」的資料，搬到靜態站之後
   那部分流量直接歸零（Pages 不算在 Supabase 頭上）。使用者已同意，還沒做。

### 已經否決的

- **只送前 N 檔**：排行榜必須用全市場個股計算。任何功能都不接受只拿部分資料運作。
  省流量只能改表示法，不能改資料量。**這條沒有例外，不要在流量壓力下重新提案。**
- **換資料庫**：這是架構問題不是廠商問題。成因是「把資料庫當 CDN 用」，任何資料庫
  都會對這件事收流量費（Neon 免費版同樣 5 GB、PlanetScale 免費版已收掉）。
  搬家要連筆記、資產、RLS、全部 migration 一起搬，搬完流量問題照樣跟過去。

### 尚未討論

- 8/25 那筆 34,880 次 REST 請求是平常的 6 倍，原因還沒查出來。
  如果是某種重試風暴，換到 CDN 之後會原封不動複製過去。

---

<a id="todo-15"></a>
## 🔵 15. D+ AI OCR：Golden Set、Validator 與正式 Worker

[↑ 回到 TODO 列表](#快速跳轉)

**狀態：AI-first 前端、正式 Supabase 私有佇列、Validator、Mac Worker、重載恢復、submit 冪等、
fallback 受控取回與獨立逾期清理已整合並發布；仍需由正式最高權限帳號做瀏覽器實際上傳驗收。
Golden Set 的身份／數量 ≥95%、成本 ≥90%、危險假陽性 0，以及公司 Windows 實機仍待驗收。依
使用者指示，不自行安裝或設定 Claude CLI。**

### 已討論

- 方向修訂為 D+ AI-first：Worker 兩分鐘內有心跳且至少一個 CLI 已登入時優先 AI；否則圖片不
  上傳，直接跑現有瀏覽器 Tesseract。Tesseract 是可用性備援，不是 AI 前置篩選器。
- AI 仍使用第二遍稽核、確定性驗證與人工確認；Tesseract fallback 必須標示來源與原因，不能
  冒充 AI 驗證結果，也不讓任一 OCR 結果直接寫入正式持倉。
- 已加入 `OcrAgentContracts`、兩個 CLI Adapter、Router、兩遍 Orchestrator、Validator、`ocr-poc` 與 `ocr-worker [--once]`。
- 主要 Agent 額度不足時改跑另一個 Agent；兩者都判定額度不足時，Router 丟
  `OcrAllAgentsQuotaExhaustedException`，外層轉為 Tesseract fallback，不改走付費 API、不忙等重試。
- AI 工作建立後才需要 fallback 時，先標成 `fallback_required`；原頁或重載後頁面都透過 owner
  驗證的 10 分鐘 signed URL 取回圖片，再執行 Tesseract，完成確認後刪除私有圖片。
- `localStorage` 只保存 job id／帳戶／檔名等非影像描述；重整後可恢復 queued／leased／succeeded，
  fallback 取回失敗則保留工作讓下一次重整再試。`db/040_ocr_hardening.sql` 加入 input hash、
  user-scoped idempotency index 與清理錯誤欄位；Supabase cron 每 5 分鐘呼叫受 secret 保護的 cleanup。
- 已新增 `OcrEngineFallbackPolicy` 與單元測試，涵蓋有效／過期心跳、沒有已登入 Agent、雙額度
  不足、兩 CLI 都不可用，以及未知程式錯誤不得被靜默偽裝成 fallback。
- 已新增 `OcrExecutionCoordinator`，把上傳前 Worker／登入預檢與 AI 執行中雙額度／無可用 Agent
  例外接到同一個 Tesseract fallback 結果；未知錯誤仍向上拋出。
- `db/039_ocr_jobs.sql`、`db/040_ocr_hardening.sql` 已套用正式 Supabase，`ocr-private` 為 private；
  anon 無法讀工作、authenticated 無法 claim。`ocr-jobs` Edge Function 已部署為手動 JWT 驗證（含
  cleanup secret）、admin／worker 分權；Supabase cron `ocr-expired-cleanup` 每 5 分鐘清理到期物件。
- Codex CLI 已用 IMG_1604 真實執行兩遍並通過 Schema；正式佇列也完成 upload／lease／result／ack／刪圖，測試工作已清除。Worker 離線三分鐘時 readiness 實測回 `worker_offline`。
- Release build 0 警告／0 錯誤，.NET 10 目前全套 394/394 通過；前端與 Edge Function 通過 JavaScript 語法檢查。

### 本輪已完成與仍待外部驗收

1. 公開 `site.js` 已由 `daily-snapshot.yml` 的 `publish-only=true` 發布並含 `ocr-jobs`、
   `fallback_required` 與重載恢復文案；仍需由正式最高權限帳號做 AI Worker 在線／離線兩條瀏覽器路徑。
2. 以 IMG_1601～1604 私有 truth 重跑至少 3 次；身份／數量 ≥95%、成本 ≥90%、危險假陽性 0 才能
   把品質標示為通過。目前 IMG_1604 的既有結果仍是 6 列、`verifiedCount=0`，不可宣稱九成。
3. 在公司 Windows 以非管理員帳號驗證 .NET 10、SecretManagement、登入時排程、鎖屏／重開機、
   網路、登入撤銷、程序重啟與 log 脫敏；Claude CLI 只有取得新的明確指示才安裝。

### 仍待實機或使用者確認

- Claude CLI 的安裝、Pro 登入、版本與 headless smoke test；完成前不宣稱雙 Agent 已可正式執行。
- 公司資安是否允許持倉截圖短暫送往 Supabase Storage 與外部 AI 供應商。
- Windows 長期運行後的 P95 延遲、訂閱額度消耗、quota recheck 間隔與 log 保存期限。
