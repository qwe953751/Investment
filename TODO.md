# 待辦事項（6 件）

這份檔案是討論的存放處，不是進度表。每次要談某件事之前先讀這裡，
就不用把前幾次的結論重講一遍。

| # | 待辦事項 | 狀態 |
|---|---|---|
| 1 | 盤中成交額預估：把分母換成量能曲線 f(t) | 🟡 等資料 |
| 2 | 盤中與盤後的比較策略 | ⚪ 未開始 |
| 3 | 族群分類（Google Sheets） | ⚪ 未開始 |
| 4 | 寫入帳號拆分：invest_writer_pc / invest_writer_ci | ⚪ 未開始 |
| 5 | 權限模型：Google 登入 + 白名單 | ⚪ 未開始 |
| 6 | Supabase Management API token 到期 | 🟡 等時間 |

狀態只有三種：🔵 進行中、🟡 等資料或等時間、⚪ 未開始。

每一項的格式固定：**已討論的結論**寫清楚為什麼這樣決定，
**尚未討論的**寫清楚卡在哪裡、需要什麼才談得下去。
結論改變就直接改這裡，不要在下面追加「更正」。

增刪項目時，標題的件數、上面的表、下面的編號要一起改。

---

## 🟡 1. 盤中成交額預估：把分母換成量能曲線 f(t)

**狀態：等資料（需要 10 個交易日，累積夠了 `status` 會提醒）**

### 已討論

現在網頁上的「預估收盤（億）」用的是線性外推：

```
預估 = 目前累計成交額 ÷ 時段進度
時段進度 = (現在 − 09:00) ÷ 270 分鐘
```

分子永遠是當下的真實累計值，每 5 分鐘重算一次就自動修正一次——
「以當前累積成交值為基準」這件事是公式本身就有的，不是額外的修正步驟。

**已知缺陷**：台股的量是 U 型的，開盤與尾盤爆量、中午乾涸。
09:00 的集合競價一次就打掉全日的 3~5%，但時間只過了 1.85%，
所以早盤會嚴重高估、中午會低估。09:27（進度 10%）之前分母太小，
一律顯示 `—`，不給數字。

**要改的是分母，不是分子**：

```
預估 = 目前累計成交額 ÷ f(t)
f(t) = 歷史上走到這個時刻時，全日成交額平均已經跑掉幾成
```

換成 f(t) 之後，U 型不再是誤差來源，而是被吸收進公式裡。

**排序策略（已定案）**：排行榜一律以**實際累計成交額**排序，
預估值只是獨立的一欄，而且**不開放排序**。
這樣早盤預估失真時，名次不會跟著亂跳——失真被關在那一欄裡。

**資料從哪來（已實作）**：`intraday_curve` 資料表，收集器每輪寫一列
（全市場成交額合計），一天約 54 列。這張表**不會**被 `sync` 刪除，
因為日內量能曲線只有盤中看得到，盤後行情裡沒有，刪掉就永遠算不回來。

f(t) 的分母用**當天最後一輪的累計值**，不用盤後正式成交值：
分子是我們自己推算的成交額（現價 × 累計量），分母也用同一套推算值，
比例才是自洽的。跟官方數字之間的系統性誤差留在曲線外面。

看曲線：`dotnet run --project src/Invest.Web -- curve`

### 尚未討論

- f(t) 到底夠不夠穩定？不同日子的曲線散布多大？大盤爆量日與量縮日的形狀一不一樣？
  這要看過 `curve` 的輸出才談得下去。
- 要用全市場的 f(t) 套用到每一檔個股，還是個股各自有自己的曲線？
  （個股的量能形狀差很多，但每檔各自估的樣本數會少很多。）
- 除權息、法說會、盤中突發新聞這種當天形狀完全走樣的日子要不要排除。
- 早盤的 10% 門檻要不要因為換了分母而放寬。

---

## ⚪ 2. 盤中與盤後的比較策略

### 尚未討論

原始需求第 5 點，明確講好「先討論、不實作」。目前**完全沒有討論過**。

要談的大方向是：盤中的即時排行怎麼跟盤後的期間排行對照才有意義。
可能的方向（都只是待選項，沒有取捨過）：

- 今天的盤中名次 vs 昨天的盤後名次，看誰在今天竄上來
- 今日預估成交額 vs 近 N 日均量，也就是把「資金加速」搬到盤中
- 盤中就標出「今天會擠進前 N 名」的個股

這一項要等預估值的準確度有底之後再談，否則所有比較都建立在會飄的數字上。

---

## ⚪ 3. 族群分類（Google Sheets）

### 已討論

分類資料放在 Google Sheets，由人維護，程式讀進來。
網頁上的說明文字已經先寫了「族群分類會在串接 Google Sheets 後加入」。

### 尚未討論

- 資料模型長什麼樣：一檔股票只屬於一個族群，還是可以掛多個標籤？
- 讀取時機：export 時抓一次寫進靜態檔，還是前端直接讀？
- 排行榜要怎麼用族群：篩選、分組、還是「族群內排名」？

---

## ⚪ 4. 寫入帳號拆分：invest_writer_pc / invest_writer_ci

### 已討論

現在本機與 GitHub Actions 共用同一組 `invest_writer` 連線字串。
拆開的好處是出事時看得出是誰寫的、可以單獨撤銷其中一組。

明確**不做**的事：`grant postgres to invest_writer`。
CI 的密碼放在 GitHub Secrets，不能讓它擁有 DDL 權限。
資料表變更一律走 Supabase Management API（見 README 的「資料表定義」）。

### 尚未討論

拆分之後 RLS policy 要怎麼寫、值不值得為此多維護一組密碼。

---

## ⚪ 5. 權限模型：Google 登入 + 白名單

### 已討論

目前靜態站是完全公開的，Supabase 的 anon key 也是公開的（只有 select 權限）。
現階段沒有機密資料，所以不急。

### 尚未討論

什麼情況下需要擋人：是因為要放自己的持股／成本，還是因為流量成本？
動機還不明確，先不設計。

---

## 🟡 6. Supabase Management API token 到期

**狀態：等時間（約 2026-09-15）**

### 已討論

`SUPABASE_ACCESS_TOKEN` 放在 `~/.zshrc`，建立於 2026-08-16，效期 30 天。
到期後只影響**資料表變更**（貼 `db/*.sql`），不影響每日回補、同步、備份——
那些走的是 `SUPABASE_DB_URL`（`invest_writer`），沒有期限。

到期後在 Supabase 後台重新產生一把，換掉 `~/.zshrc` 裡那一行即可。
